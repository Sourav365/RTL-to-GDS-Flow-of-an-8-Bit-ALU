library("fsa0m_a_generic_cores") {
  SCALE = 1.0 / 16.0;
  ROUTE_GRID = 1024;
  set_route_grid(ROUTE_GRID);
  set_meter_scale(0.00254 / (16 * SCALE * ROUTE_GRID));
  symbol ("AN2") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AN2" */
  symbol ("AN2B1") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AN2B1" */
  symbol ("AN2B1P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AN2B1P" */
  symbol ("AN2B1S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AN2B1S" */
  symbol ("AN2B1T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AN2B1T" */
  symbol ("AN2P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AN2P" */
  symbol ("AN2S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AN2S" */
  symbol ("AN2T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AN2T" */
  symbol ("AN3") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AN3" */
  symbol ("AN3B1") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AN3B1" */
  symbol ("AN3B1P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AN3B1P" */
  symbol ("AN3B1S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AN3B1S" */
  symbol ("AN3B1T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AN3B1T" */
  symbol ("AN3B2") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (16 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    circle (24 * SCALE, -48 * SCALE, 8 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "AN3B2" */
  symbol ("AN3B2P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (16 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    circle (24 * SCALE, -48 * SCALE, 8 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "AN3B2P" */
  symbol ("AN3B2S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (16 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    circle (24 * SCALE, -48 * SCALE, 8 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "AN3B2S" */
  symbol ("AN3B2T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    circle (24 * SCALE, -48 * SCALE, 8 * SCALE);
    circle (24 * SCALE, -80 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "AN3B2T" */
  symbol ("AN3P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AN3P" */
  symbol ("AN3S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AN3S" */
  symbol ("AN3T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (0 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AN3T" */
  symbol ("AN4") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "AN4" */
  symbol ("AN4B1") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (32 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (32 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -96 * SCALE, 8 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AN4B1" */
  symbol ("AN4B1P") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 176 * SCALE, 0 * SCALE);
    line (0 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (32 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (32 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (32 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -96 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AN4B1P" */
  symbol ("AN4B1S") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 176 * SCALE, 0 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (32 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (32 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -96 * SCALE, 8 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AN4B1S" */
  symbol ("AN4B1T") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 176 * SCALE, 0 * SCALE);
    line (0 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (32 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (32 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (32 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -96 * SCALE, 8 * SCALE);
    line (176 * SCALE, -48 * SCALE, 156 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (32 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    arc (108 * SCALE, 0 * SCALE, 108 * SCALE, -96 * SCALE,\
        108 * SCALE, -48 * SCALE);
    pin ("O", 176 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AN4B1T" */
  symbol ("AN4P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "AN4P" */
  symbol ("AN4S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "AN4S" */
  symbol ("AN4T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (160 * SCALE, -48 * SCALE, 140 * SCALE, -48 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (0 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "AN4T" */
  symbol ("ANTENNA") {
    set_minimum_boundary (-16 * SCALE, -64 * SCALE, 80 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, 0 * SCALE, -64 * SCALE);
    line (0 * SCALE, 0 * SCALE, 0 * SCALE, -32 * SCALE);
    line (0 * SCALE, -16 * SCALE, 0 * SCALE, 0 * SCALE);
    line (32 * SCALE, -32 * SCALE, 32 * SCALE, -64 * SCALE);
    line (64 * SCALE, -52 * SCALE, 56 * SCALE, -44 * SCALE);
    line (72 * SCALE, -44 * SCALE, 64 * SCALE, -52 * SCALE);
    line (56 * SCALE, -44 * SCALE, 72 * SCALE, -44 * SCALE);
    line (64 * SCALE, -32 * SCALE, 64 * SCALE, -44 * SCALE);
    line (28 * SCALE, -32 * SCALE, 64 * SCALE, -32 * SCALE);
    line (28 * SCALE, -16 * SCALE, 28 * SCALE, -44 * SCALE);
    line (32 * SCALE, -64 * SCALE, 0 * SCALE, -32 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -32 * SCALE);
    line (0 * SCALE, -32 * SCALE, 32 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 0 * SCALE, -44 * SCALE);
    line (-16 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    pin ("A", -16 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "ANTENNA" */
  symbol ("AO112") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    arc (224 * SCALE, -80 * SCALE, 160 * SCALE, -132 * SCALE,\
        158.768 * SCALE, -65.099 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (32 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (108 * SCALE, -132 * SCALE, 160 * SCALE, -132 * SCALE);
    arc (104 * SCALE, -28 * SCALE, 104 * SCALE, -132 * SCALE,\
        69.7143 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (32 * SCALE, -112 * SCALE, 124 * SCALE, -112 * SCALE);
    line (128 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -28 * SCALE, 160 * SCALE, -28 * SCALE);
    arc (160 * SCALE, -28 * SCALE, 224 * SCALE, -80 * SCALE,\
        158.768 * SCALE, -94.901 * SCALE);
    pin ("O", 240 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AO112" */
  symbol ("AO112P") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    arc (224 * SCALE, -80 * SCALE, 160 * SCALE, -132 * SCALE,\
        158.768 * SCALE, -65.099 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (32 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (108 * SCALE, -132 * SCALE, 160 * SCALE, -132 * SCALE);
    arc (104 * SCALE, -28 * SCALE, 104 * SCALE, -132 * SCALE,\
        69.7143 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (32 * SCALE, -112 * SCALE, 124 * SCALE, -112 * SCALE);
    line (128 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -28 * SCALE, 160 * SCALE, -28 * SCALE);
    arc (160 * SCALE, -28 * SCALE, 224 * SCALE, -80 * SCALE,\
        158.768 * SCALE, -94.901 * SCALE);
    pin ("O", 240 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AO112P" */
  symbol ("AO112S") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 240 * SCALE, 16 * SCALE);
    line (224 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    arc (224 * SCALE, -80 * SCALE, 160 * SCALE, -132 * SCALE,\
        158.768 * SCALE, -65.099 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (32 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (108 * SCALE, -132 * SCALE, 160 * SCALE, -132 * SCALE);
    arc (104 * SCALE, -28 * SCALE, 104 * SCALE, -132 * SCALE,\
        69.7143 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (32 * SCALE, -112 * SCALE, 124 * SCALE, -112 * SCALE);
    line (128 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -28 * SCALE, 160 * SCALE, -28 * SCALE);
    arc (160 * SCALE, -28 * SCALE, 224 * SCALE, -80 * SCALE,\
        158.768 * SCALE, -94.901 * SCALE);
    pin ("O", 240 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AO112S" */
  symbol ("AO112T") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    arc (224 * SCALE, -80 * SCALE, 160 * SCALE, -132 * SCALE,\
        158.768 * SCALE, -65.099 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (32 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (108 * SCALE, -132 * SCALE, 160 * SCALE, -132 * SCALE);
    arc (104 * SCALE, -28 * SCALE, 104 * SCALE, -132 * SCALE,\
        69.7143 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (32 * SCALE, -112 * SCALE, 124 * SCALE, -112 * SCALE);
    line (128 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -28 * SCALE, 160 * SCALE, -28 * SCALE);
    arc (160 * SCALE, -28 * SCALE, 224 * SCALE, -80 * SCALE,\
        158.768 * SCALE, -94.901 * SCALE);
    pin ("O", 240 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AO112T" */
  symbol ("AO12") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -64 * SCALE, 240 * SCALE, -64 * SCALE);
    line (112 * SCALE, -48 * SCALE, 128 * SCALE, -48 * SCALE);
    line (48 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (48 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (120 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 80 * SCALE, -80 * SCALE,\
        80 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 48 * SCALE, -80 * SCALE);
    line (48 * SCALE, -80 * SCALE, 80 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 48 * SCALE, -16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 240 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AO12" */
  symbol ("AO12P") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -64 * SCALE, 240 * SCALE, -64 * SCALE);
    line (112 * SCALE, -48 * SCALE, 128 * SCALE, -48 * SCALE);
    line (48 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (48 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (120 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 80 * SCALE, -80 * SCALE,\
        80 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 48 * SCALE, -80 * SCALE);
    line (48 * SCALE, -80 * SCALE, 80 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 48 * SCALE, -16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 240 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AO12P" */
  symbol ("AO12S") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -64 * SCALE, 240 * SCALE, -64 * SCALE);
    line (112 * SCALE, -48 * SCALE, 128 * SCALE, -48 * SCALE);
    line (48 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (48 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (120 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 80 * SCALE, -80 * SCALE,\
        80 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 48 * SCALE, -80 * SCALE);
    line (48 * SCALE, -80 * SCALE, 80 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 48 * SCALE, -16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 240 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AO12S" */
  symbol ("AO12T") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -64 * SCALE, 240 * SCALE, -64 * SCALE);
    line (112 * SCALE, -48 * SCALE, 128 * SCALE, -48 * SCALE);
    line (48 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (48 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (120 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 80 * SCALE, -80 * SCALE,\
        80 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 48 * SCALE, -80 * SCALE);
    line (48 * SCALE, -80 * SCALE, 80 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 48 * SCALE, -16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 240 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AO12T" */
  symbol ("AO13") {
    set_minimum_boundary (-48 * SCALE, -144 * SCALE, 256 * SCALE, 32 * SCALE);
    line (256 * SCALE, -64 * SCALE, 224 * SCALE, -64 * SCALE);
    line (120 * SCALE, -96 * SCALE, -48 * SCALE, -96 * SCALE);
    line (0 * SCALE, 0 * SCALE, -48 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, -48 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 0 * SCALE, 16 * SCALE);
    line (0 * SCALE, -64 * SCALE, -48 * SCALE, -64 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 64 * SCALE, -80 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 64 * SCALE, -80 * SCALE);
    line (0 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B3", -48 * SCALE, -64 * SCALE, LEFT);
    pin ("B2", -48 * SCALE, -32 * SCALE, LEFT);
    pin ("B1", -48 * SCALE, 0 * SCALE, LEFT);
    pin ("A1", -48 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AO13" */
  symbol ("AO13P") {
    set_minimum_boundary (-48 * SCALE, -144 * SCALE, 256 * SCALE, 32 * SCALE);
    line (256 * SCALE, -64 * SCALE, 224 * SCALE, -64 * SCALE);
    line (120 * SCALE, -96 * SCALE, -48 * SCALE, -96 * SCALE);
    line (0 * SCALE, 0 * SCALE, -48 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, -48 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 0 * SCALE, 16 * SCALE);
    line (0 * SCALE, -64 * SCALE, -48 * SCALE, -64 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 64 * SCALE, -80 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 64 * SCALE, -80 * SCALE);
    line (0 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B3", -48 * SCALE, -64 * SCALE, LEFT);
    pin ("B2", -48 * SCALE, -32 * SCALE, LEFT);
    pin ("B1", -48 * SCALE, 0 * SCALE, LEFT);
    pin ("A1", -48 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AO13P" */
  symbol ("AO13S") {
    set_minimum_boundary (-48 * SCALE, -144 * SCALE, 256 * SCALE, 32 * SCALE);
    line (256 * SCALE, -64 * SCALE, 224 * SCALE, -64 * SCALE);
    line (120 * SCALE, -96 * SCALE, -48 * SCALE, -96 * SCALE);
    line (0 * SCALE, 0 * SCALE, -48 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, -48 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 0 * SCALE, 16 * SCALE);
    line (0 * SCALE, -64 * SCALE, -48 * SCALE, -64 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 64 * SCALE, -80 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 64 * SCALE, -80 * SCALE);
    line (0 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B3", -48 * SCALE, -64 * SCALE, LEFT);
    pin ("B2", -48 * SCALE, -32 * SCALE, LEFT);
    pin ("B1", -48 * SCALE, 0 * SCALE, LEFT);
    pin ("A1", -48 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AO13S" */
  symbol ("AO13T") {
    set_minimum_boundary (-48 * SCALE, -144 * SCALE, 256 * SCALE, 32 * SCALE);
    line (256 * SCALE, -64 * SCALE, 224 * SCALE, -64 * SCALE);
    line (120 * SCALE, -96 * SCALE, -48 * SCALE, -96 * SCALE);
    line (0 * SCALE, 0 * SCALE, -48 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, -48 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 0 * SCALE, 16 * SCALE);
    line (0 * SCALE, -64 * SCALE, -48 * SCALE, -64 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 64 * SCALE, -80 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 64 * SCALE, -80 * SCALE);
    line (0 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B3", -48 * SCALE, -64 * SCALE, LEFT);
    pin ("B2", -48 * SCALE, -32 * SCALE, LEFT);
    pin ("B1", -48 * SCALE, 0 * SCALE, LEFT);
    pin ("A1", -48 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AO13T" */
  symbol ("AO22") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -64 * SCALE, 240 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    pin ("O", 240 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AO22" */
  symbol ("AO222") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -96 * SCALE, 240 * SCALE, -96 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (48 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -96 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AO222" */
  symbol ("AO222P") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -96 * SCALE, 240 * SCALE, -96 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (48 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -96 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AO222P" */
  symbol ("AO222S") {
    set_minimum_boundary (0 * SCALE, -208 * SCALE, 240 * SCALE, 16 * SCALE);
    line (224 * SCALE, -96 * SCALE, 240 * SCALE, -96 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (48 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -96 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AO222S" */
  symbol ("AO222T") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -96 * SCALE, 240 * SCALE, -96 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (48 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -96 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AO222T" */
  symbol ("AO22P") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -64 * SCALE, 240 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    pin ("O", 240 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AO22P" */
  symbol ("AO22S") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 240 * SCALE, 16 * SCALE);
    line (224 * SCALE, -64 * SCALE, 240 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    pin ("O", 240 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AO22S" */
  symbol ("AO22T") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -64 * SCALE, 240 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    pin ("O", 240 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AO22T" */
  symbol ("AOI112H") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (32 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (108 * SCALE, -132 * SCALE, 160 * SCALE, -132 * SCALE);
    arc (104 * SCALE, -28 * SCALE, 104 * SCALE, -132 * SCALE,\
        69.7143 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (224 * SCALE, -80 * SCALE, 160 * SCALE, -132 * SCALE,\
        158.768 * SCALE, -65.099 * SCALE);
    line (48 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (32 * SCALE, -112 * SCALE, 124 * SCALE, -112 * SCALE);
    line (128 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (108 * SCALE, -28 * SCALE, 160 * SCALE, -28 * SCALE);
    arc (160 * SCALE, -28 * SCALE, 224 * SCALE, -80 * SCALE,\
        158.768 * SCALE, -94.901 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AOI112H" */
  symbol ("AOI112HP") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (32 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (108 * SCALE, -132 * SCALE, 160 * SCALE, -132 * SCALE);
    arc (104 * SCALE, -28 * SCALE, 104 * SCALE, -132 * SCALE,\
        69.7143 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (224 * SCALE, -80 * SCALE, 160 * SCALE, -132 * SCALE,\
        158.768 * SCALE, -65.099 * SCALE);
    line (48 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (32 * SCALE, -112 * SCALE, 124 * SCALE, -112 * SCALE);
    line (128 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (108 * SCALE, -28 * SCALE, 160 * SCALE, -28 * SCALE);
    arc (160 * SCALE, -28 * SCALE, 224 * SCALE, -80 * SCALE,\
        158.768 * SCALE, -94.901 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AOI112HP" */
  symbol ("AOI112HS") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (32 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (108 * SCALE, -132 * SCALE, 160 * SCALE, -132 * SCALE);
    arc (104 * SCALE, -28 * SCALE, 104 * SCALE, -132 * SCALE,\
        69.7143 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (224 * SCALE, -80 * SCALE, 160 * SCALE, -132 * SCALE,\
        158.768 * SCALE, -65.099 * SCALE);
    line (48 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (32 * SCALE, -112 * SCALE, 124 * SCALE, -112 * SCALE);
    line (128 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (108 * SCALE, -28 * SCALE, 160 * SCALE, -28 * SCALE);
    arc (160 * SCALE, -28 * SCALE, 224 * SCALE, -80 * SCALE,\
        158.768 * SCALE, -94.901 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AOI112HS" */
  symbol ("AOI112HT") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (32 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (108 * SCALE, -132 * SCALE, 160 * SCALE, -132 * SCALE);
    arc (104 * SCALE, -28 * SCALE, 104 * SCALE, -132 * SCALE,\
        69.7143 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (224 * SCALE, -80 * SCALE, 160 * SCALE, -132 * SCALE,\
        158.768 * SCALE, -65.099 * SCALE);
    line (48 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (32 * SCALE, -112 * SCALE, 124 * SCALE, -112 * SCALE);
    line (128 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (108 * SCALE, -28 * SCALE, 160 * SCALE, -28 * SCALE);
    arc (160 * SCALE, -28 * SCALE, 224 * SCALE, -80 * SCALE,\
        158.768 * SCALE, -94.901 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "AOI112HT" */
  symbol ("AOI12H") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -48 * SCALE, 128 * SCALE, -48 * SCALE);
    line (48 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (48 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (120 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 80 * SCALE, -80 * SCALE,\
        80 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 48 * SCALE, -80 * SCALE);
    line (48 * SCALE, -80 * SCALE, 80 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 48 * SCALE, -16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AOI12H" */
  symbol ("AOI12HP") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -48 * SCALE, 128 * SCALE, -48 * SCALE);
    line (48 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (48 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (120 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 80 * SCALE, -80 * SCALE,\
        80 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 48 * SCALE, -80 * SCALE);
    line (48 * SCALE, -80 * SCALE, 80 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 48 * SCALE, -16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AOI12HP" */
  symbol ("AOI12HS") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -48 * SCALE, 128 * SCALE, -48 * SCALE);
    line (48 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (48 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (120 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 80 * SCALE, -80 * SCALE,\
        80 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 48 * SCALE, -80 * SCALE);
    line (48 * SCALE, -80 * SCALE, 80 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 48 * SCALE, -16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AOI12HS" */
  symbol ("AOI12HT") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -48 * SCALE, 128 * SCALE, -48 * SCALE);
    line (48 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    line (48 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (120 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 80 * SCALE, -80 * SCALE,\
        80 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 48 * SCALE, -80 * SCALE);
    line (48 * SCALE, -80 * SCALE, 80 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 48 * SCALE, -16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AOI12HT" */
  symbol ("AOI13H") {
    set_minimum_boundary (-48 * SCALE, -144 * SCALE, 256 * SCALE, 16 * SCALE);
    line (120 * SCALE, -96 * SCALE, -48 * SCALE, -96 * SCALE);
    line (0 * SCALE, 0 * SCALE, -48 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, -48 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 0 * SCALE, 16 * SCALE);
    line (0 * SCALE, -64 * SCALE, -48 * SCALE, -64 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 64 * SCALE, -80 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 64 * SCALE, -80 * SCALE);
    line (0 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B3", -48 * SCALE, -64 * SCALE, LEFT);
    pin ("B2", -48 * SCALE, -32 * SCALE, LEFT);
    pin ("B1", -48 * SCALE, 0 * SCALE, LEFT);
    pin ("A1", -48 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AOI13H" */
  symbol ("AOI13HP") {
    set_minimum_boundary (-48 * SCALE, -144 * SCALE, 256 * SCALE, 16 * SCALE);
    line (120 * SCALE, -96 * SCALE, -48 * SCALE, -96 * SCALE);
    line (0 * SCALE, 0 * SCALE, -48 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, -48 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 0 * SCALE, 16 * SCALE);
    line (0 * SCALE, -64 * SCALE, -48 * SCALE, -64 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 64 * SCALE, -80 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 64 * SCALE, -80 * SCALE);
    line (0 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B3", -48 * SCALE, -64 * SCALE, LEFT);
    pin ("B2", -48 * SCALE, -32 * SCALE, LEFT);
    pin ("B1", -48 * SCALE, 0 * SCALE, LEFT);
    pin ("A1", -48 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AOI13HP" */
  symbol ("AOI13HS") {
    set_minimum_boundary (-48 * SCALE, -144 * SCALE, 256 * SCALE, 16 * SCALE);
    line (120 * SCALE, -96 * SCALE, -48 * SCALE, -96 * SCALE);
    line (0 * SCALE, 0 * SCALE, -48 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, -48 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 0 * SCALE, 16 * SCALE);
    line (0 * SCALE, -64 * SCALE, -48 * SCALE, -64 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 64 * SCALE, -80 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 64 * SCALE, -80 * SCALE);
    line (0 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B3", -48 * SCALE, -64 * SCALE, LEFT);
    pin ("B2", -48 * SCALE, -32 * SCALE, LEFT);
    pin ("B1", -48 * SCALE, 0 * SCALE, LEFT);
    pin ("A1", -48 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AOI13HS" */
  symbol ("AOI13HT") {
    set_minimum_boundary (-48 * SCALE, -144 * SCALE, 256 * SCALE, 16 * SCALE);
    line (120 * SCALE, -96 * SCALE, -48 * SCALE, -96 * SCALE);
    line (0 * SCALE, 0 * SCALE, -48 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, -48 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 0 * SCALE, 16 * SCALE);
    line (0 * SCALE, -64 * SCALE, -48 * SCALE, -64 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 64 * SCALE, -80 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 64 * SCALE, -80 * SCALE);
    line (0 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B3", -48 * SCALE, -64 * SCALE, LEFT);
    pin ("B2", -48 * SCALE, -32 * SCALE, LEFT);
    pin ("B1", -48 * SCALE, 0 * SCALE, LEFT);
    pin ("A1", -48 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "AOI13HT" */
  symbol ("AOI222H") {
    set_minimum_boundary (0 * SCALE, -208 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (48 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -96 * SCALE, 256 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -96 * SCALE, 8 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -96 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AOI222H" */
  symbol ("AOI222HP") {
    set_minimum_boundary (0 * SCALE, -208 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (48 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -96 * SCALE, 256 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -96 * SCALE, 8 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -96 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AOI222HP" */
  symbol ("AOI222HS") {
    set_minimum_boundary (0 * SCALE, -208 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (48 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -96 * SCALE, 256 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -96 * SCALE, 8 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -96 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AOI222HS" */
  symbol ("AOI22H") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AOI22H" */
  symbol ("AOI22HP") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AOI22HP" */
  symbol ("AOI22HT") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AOI22HT" */
  symbol ("AOI22S") {
    set_minimum_boundary (0 * SCALE, -144 * SCALE, 256 * SCALE, 16 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (48 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (48 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "AOI22S" */
  symbol ("BHD1") {
    set_minimum_boundary (-128 * SCALE, -128 * SCALE, 96 * SCALE, 48 * SCALE);
    line (-128 * SCALE, -128 * SCALE, -128 * SCALE, 48 * SCALE);
    line (-128 * SCALE, 48 * SCALE, 96 * SCALE, 48 * SCALE);
    line (96 * SCALE, 48 * SCALE, 96 * SCALE, -128 * SCALE);
    line (96 * SCALE, -128 * SCALE, -128 * SCALE, -128 * SCALE);
    line (-128 * SCALE, -80 * SCALE, -64 * SCALE, -80 * SCALE);
    line (-100 * SCALE, -84 * SCALE, -100 * SCALE, -76 * SCALE);
    line (-100 * SCALE, -76 * SCALE, -92 * SCALE, -76 * SCALE);
    line (-92 * SCALE, -76 * SCALE, -92 * SCALE, -84 * SCALE);
    line (-92 * SCALE, -84 * SCALE, -100 * SCALE, -84 * SCALE);
    line (64 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (64 * SCALE, 0 * SCALE, 64 * SCALE, -80 * SCALE);
    line (32 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (-96 * SCALE, 0 * SCALE, -96 * SCALE, -80 * SCALE);
    line (-64 * SCALE, 0 * SCALE, -96 * SCALE, 0 * SCALE);
    circle (16 * SCALE, -80 * SCALE, 8 * SCALE);
    line (32 * SCALE, -80 * SCALE, 24 * SCALE, -80 * SCALE);
    line (8 * SCALE, -80 * SCALE, -64 * SCALE, -48 * SCALE);
    line (-64 * SCALE, -112 * SCALE, 8 * SCALE, -80 * SCALE);
    line (-64 * SCALE, -48 * SCALE, -64 * SCALE, -112 * SCALE);
    circle (-48 * SCALE, 0 * SCALE, 8 * SCALE);
    line (-64 * SCALE, 0 * SCALE, -56 * SCALE, 0 * SCALE);
    line (-40 * SCALE, 0 * SCALE, 32 * SCALE, 32 * SCALE);
    line (32 * SCALE, -32 * SCALE, -40 * SCALE, 0 * SCALE);
    line (32 * SCALE, 32 * SCALE, 32 * SCALE, -32 * SCALE);
    line (-128 * SCALE, 48 * SCALE, -128 * SCALE, 32 * SCALE);
    pin ("H", -128 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "BHD1" */
  symbol ("BUF1") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF1" */
  symbol ("BUF12CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF12CK" */
  symbol ("BUF1CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF1CK" */
  symbol ("BUF1S") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF1S" */
  symbol ("BUF2") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF2" */
  symbol ("BUF2CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF2CK" */
  symbol ("BUF3") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF3" */
  symbol ("BUF3CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF3CK" */
  symbol ("BUF4") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF4" */
  symbol ("BUF4CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF4CK" */
  symbol ("BUF6") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF6" */
  symbol ("BUF6CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF6CK" */
  symbol ("BUF8") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF8" */
  symbol ("BUF8CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "BUF8CK" */
  symbol ("BUFB1") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (48 * SCALE, -68 * SCALE, 48 * SCALE, -80 * SCALE);
    circle (48 * SCALE, -60 * SCALE, 8 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("EB", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "BUFB1" */
  symbol ("BUFB2") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (48 * SCALE, -68 * SCALE, 48 * SCALE, -80 * SCALE);
    circle (48 * SCALE, -60 * SCALE, 8 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("EB", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "BUFB2" */
  symbol ("BUFB3") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (48 * SCALE, -68 * SCALE, 48 * SCALE, -80 * SCALE);
    circle (48 * SCALE, -60 * SCALE, 8 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("EB", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "BUFB3" */
  symbol ("BUFT1") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (48 * SCALE, -80 * SCALE, 48 * SCALE, -48 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("E", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "BUFT1" */
  symbol ("BUFT2") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (48 * SCALE, -80 * SCALE, 48 * SCALE, -48 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("E", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "BUFT2" */
  symbol ("BUFT3") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (48 * SCALE, -80 * SCALE, 48 * SCALE, -48 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("E", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "BUFT3" */
  symbol ("BUFT4") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (48 * SCALE, -80 * SCALE, 48 * SCALE, -48 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("E", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "BUFT4" */
  symbol ("CMPE4") {
    set_minimum_boundary (0 * SCALE, -352 * SCALE, 288 * SCALE, 0 * SCALE);
    line (256 * SCALE, -176 * SCALE, 288 * SCALE, -176 * SCALE);
    line (32 * SCALE, -352 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 256 * SCALE, 0 * SCALE);
    line (256 * SCALE, 0 * SCALE, 256 * SCALE, -352 * SCALE);
    line (256 * SCALE, -352 * SCALE, 32 * SCALE, -352 * SCALE);
    line (0 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (0 * SCALE, -256 * SCALE, 32 * SCALE, -256 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("OEQ", 288 * SCALE, -176 * SCALE, RIGHT);
    pin ("B3", 0 * SCALE, -288 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("B0", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A3", 0 * SCALE, -256 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("A0", 0 * SCALE, -64 * SCALE, LEFT);
  } /* end of symbol "CMPE4" */
  symbol ("CMPE4S") {
    set_minimum_boundary (0 * SCALE, -352 * SCALE, 288 * SCALE, 0 * SCALE);
    line (256 * SCALE, -176 * SCALE, 288 * SCALE, -176 * SCALE);
    line (32 * SCALE, -352 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 256 * SCALE, 0 * SCALE);
    line (256 * SCALE, 0 * SCALE, 256 * SCALE, -352 * SCALE);
    line (256 * SCALE, -352 * SCALE, 32 * SCALE, -352 * SCALE);
    line (0 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (0 * SCALE, -256 * SCALE, 32 * SCALE, -256 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    pin ("OEQ", 288 * SCALE, -176 * SCALE, RIGHT);
    pin ("B3", 0 * SCALE, -288 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("B0", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A3", 0 * SCALE, -256 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("A0", 0 * SCALE, -64 * SCALE, LEFT);
  } /* end of symbol "CMPE4S" */
  symbol ("DBFRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -176 * SCALE, 16 * SCALE, -176 * SCALE);
    circle (24 * SCALE, -176 * SCALE, 8 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CKB", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DBFRBN" */
  symbol ("DBFRSBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -176 * SCALE, 16 * SCALE, -176 * SCALE);
    circle (24 * SCALE, -176 * SCALE, 8 * SCALE);
    circle (112 * SCALE, -24 * SCALE, 8 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("SB", 112 * SCALE, 0 * SCALE, UP);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CKB", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DBFRSBN" */
  symbol ("DBHRBN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (0 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CKB", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DBHRBN" */
  symbol ("DBHRBS") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (0 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CKB", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DBHRBS" */
  symbol ("DBZRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (192 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 16 * SCALE, -176 * SCALE);
    circle (24 * SCALE, -176 * SCALE, 8 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CKB", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DBZRBN" */
  symbol ("DBZRSBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, 0 * SCALE);
    line (16 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    circle (24 * SCALE, -176 * SCALE, 8 * SCALE);
    circle (112 * SCALE, -24 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("SB", 112 * SCALE, 0 * SCALE, UP);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CKB", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DBZRSBN" */
  symbol ("DELA") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (128 * SCALE, -48 * SCALE, 160 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 128 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 128 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "DELA" */
  symbol ("DELB") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (128 * SCALE, -48 * SCALE, 160 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 128 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 128 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "DELB" */
  symbol ("DELC") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (128 * SCALE, -48 * SCALE, 160 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 128 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 128 * SCALE, -48 * SCALE);
    line (32 * SCALE, 0 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "DELC" */
  symbol ("DFCLRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("LD", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFCLRBN" */
  symbol ("DFCRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFCRBN" */
  symbol ("DFFN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFFN" */
  symbol ("DFFP") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFFP" */
  symbol ("DFFRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFFRBN" */
  symbol ("DFFRBP") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFFRBP" */
  symbol ("DFFRBS") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFFRBS" */
  symbol ("DFFRBT") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFFRBT" */
  symbol ("DFFRSBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -24 * SCALE, 8 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("SB", 112 * SCALE, 0 * SCALE, UP);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFFRSBN" */
  symbol ("DFFS") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFFS" */
  symbol ("DFFSBN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -24 * SCALE, 8 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("SB", 112 * SCALE, 0 * SCALE, UP);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFFSBN" */
  symbol ("DFTRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (224 * SCALE, -176 * SCALE, 192 * SCALE, -176 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -240 * SCALE, 112 * SCALE, -256 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QZ", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("E", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFTRBN" */
  symbol ("DFTRBS") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (224 * SCALE, -176 * SCALE, 192 * SCALE, -176 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -240 * SCALE, 112 * SCALE, -256 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QZ", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("E", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFTRBS" */
  symbol ("DFZCLRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 32 * SCALE, -208 * SCALE);
    line (32 * SCALE, -176 * SCALE, 48 * SCALE, -192 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("LD", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -192 * SCALE, LEFT);
  } /* end of symbol "DFZCLRBN" */
  symbol ("DFZCRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFZCRBN" */
  symbol ("DFZN") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -128 * SCALE, 48 * SCALE, -144 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DFZN" */
  symbol ("DFZP") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -128 * SCALE, 48 * SCALE, -144 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DFZP" */
  symbol ("DFZRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFZRBN" */
  symbol ("DFZRBP") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFZRBP" */
  symbol ("DFZRBS") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFZRBS" */
  symbol ("DFZRBT") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFZRBT" */
  symbol ("DFZRSBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, 0 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    circle (112 * SCALE, -24 * SCALE, 8 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("SB", 112 * SCALE, 0 * SCALE, UP);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFZRSBN" */
  symbol ("DFZS") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -128 * SCALE, 48 * SCALE, -144 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DFZS" */
  symbol ("DFZSBN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    circle (112 * SCALE, -24 * SCALE, 8 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("SB", 112 * SCALE, 0 * SCALE, UP);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "DFZSBN" */
  symbol ("DFZTRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (48 * SCALE, -192 * SCALE, 32 * SCALE, -208 * SCALE);
    line (32 * SCALE, -176 * SCALE, 48 * SCALE, -192 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (224 * SCALE, -176 * SCALE, 192 * SCALE, -176 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QZ", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("E", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -192 * SCALE, LEFT);
  } /* end of symbol "DFZTRBN" */
  symbol ("DFZTRBS") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (48 * SCALE, -192 * SCALE, 32 * SCALE, -208 * SCALE);
    line (32 * SCALE, -176 * SCALE, 48 * SCALE, -192 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (224 * SCALE, -176 * SCALE, 192 * SCALE, -176 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("QZ", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("E", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -192 * SCALE, LEFT);
  } /* end of symbol "DFZTRBS" */
  symbol ("DLHN") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DLHN" */
  symbol ("DLHP") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DLHP" */
  symbol ("DLHRBN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DLHRBN" */
  symbol ("DLHRBP") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DLHRBP" */
  symbol ("DLHRBS") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DLHRBS" */
  symbol ("DLHS") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "DLHS" */
  symbol ("FA1") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA1" */
  symbol ("FA1P") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA1P" */
  symbol ("FA1S") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA1S" */
  symbol ("FA1T") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA1T" */
  symbol ("FA2") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (16 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA2" */
  symbol ("FA2P") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (16 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA2P" */
  symbol ("FA2S") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (16 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA2S" */
  symbol ("FA3") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, -144 * SCALE, 208 * SCALE, -144 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA3" */
  symbol ("FA3P") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, -144 * SCALE, 208 * SCALE, -144 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA3P" */
  symbol ("FA3S") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, -144 * SCALE, 208 * SCALE, -144 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CI", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FA3S" */
  symbol ("FACS1") {
    set_minimum_boundary (0 * SCALE, -272 * SCALE, 224 * SCALE, -16 * SCALE);
    line (224 * SCALE, -240 * SCALE, 192 * SCALE, -240 * SCALE);
    line (224 * SCALE, -144 * SCALE, 192 * SCALE, -144 * SCALE);
    circle (24 * SCALE, -240 * SCALE, 8 * SCALE);
    circle (24 * SCALE, -192 * SCALE, 8 * SCALE);
    line (16 * SCALE, -192 * SCALE, 0 * SCALE, -192 * SCALE);
    line (16 * SCALE, -240 * SCALE, 0 * SCALE, -240 * SCALE);
    line (32 * SCALE, -272 * SCALE, 32 * SCALE, -16 * SCALE);
    line (32 * SCALE, -16 * SCALE, 192 * SCALE, -16 * SCALE);
    line (192 * SCALE, -16 * SCALE, 192 * SCALE, -272 * SCALE);
    line (192 * SCALE, -272 * SCALE, 32 * SCALE, -272 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO1", 224 * SCALE, -240 * SCALE, RIGHT);
    pin ("CO0", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CS", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("CI1", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("CI0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FACS1" */
  symbol ("FACS1P") {
    set_minimum_boundary (0 * SCALE, -272 * SCALE, 224 * SCALE, -16 * SCALE);
    line (224 * SCALE, -240 * SCALE, 192 * SCALE, -240 * SCALE);
    line (224 * SCALE, -144 * SCALE, 192 * SCALE, -144 * SCALE);
    circle (24 * SCALE, -240 * SCALE, 8 * SCALE);
    circle (24 * SCALE, -192 * SCALE, 8 * SCALE);
    line (16 * SCALE, -192 * SCALE, 0 * SCALE, -192 * SCALE);
    line (16 * SCALE, -240 * SCALE, 0 * SCALE, -240 * SCALE);
    line (32 * SCALE, -272 * SCALE, 32 * SCALE, -16 * SCALE);
    line (32 * SCALE, -16 * SCALE, 192 * SCALE, -16 * SCALE);
    line (192 * SCALE, -16 * SCALE, 192 * SCALE, -272 * SCALE);
    line (192 * SCALE, -272 * SCALE, 32 * SCALE, -272 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO1", 224 * SCALE, -240 * SCALE, RIGHT);
    pin ("CO0", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CS", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("CI1", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("CI0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FACS1P" */
  symbol ("FACS1S") {
    set_minimum_boundary (0 * SCALE, -272 * SCALE, 224 * SCALE, -16 * SCALE);
    line (224 * SCALE, -240 * SCALE, 192 * SCALE, -240 * SCALE);
    line (224 * SCALE, -144 * SCALE, 192 * SCALE, -144 * SCALE);
    circle (24 * SCALE, -240 * SCALE, 8 * SCALE);
    circle (24 * SCALE, -192 * SCALE, 8 * SCALE);
    line (16 * SCALE, -192 * SCALE, 0 * SCALE, -192 * SCALE);
    line (16 * SCALE, -240 * SCALE, 0 * SCALE, -240 * SCALE);
    line (32 * SCALE, -272 * SCALE, 32 * SCALE, -16 * SCALE);
    line (32 * SCALE, -16 * SCALE, 192 * SCALE, -16 * SCALE);
    line (192 * SCALE, -16 * SCALE, 192 * SCALE, -272 * SCALE);
    line (192 * SCALE, -272 * SCALE, 32 * SCALE, -272 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO1", 224 * SCALE, -240 * SCALE, RIGHT);
    pin ("CO0", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CS", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("CI1", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("CI0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FACS1S" */
  symbol ("FACS2") {
    set_minimum_boundary (0 * SCALE, -272 * SCALE, 224 * SCALE, -16 * SCALE);
    line (224 * SCALE, -240 * SCALE, 208 * SCALE, -240 * SCALE);
    circle (200 * SCALE, -240 * SCALE, 8 * SCALE);
    line (224 * SCALE, -144 * SCALE, 208 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -272 * SCALE, 32 * SCALE, -16 * SCALE);
    line (32 * SCALE, -16 * SCALE, 192 * SCALE, -16 * SCALE);
    line (192 * SCALE, -16 * SCALE, 192 * SCALE, -272 * SCALE);
    line (192 * SCALE, -272 * SCALE, 32 * SCALE, -272 * SCALE);
    line (32 * SCALE, -240 * SCALE, 0 * SCALE, -240 * SCALE);
    line (32 * SCALE, -192 * SCALE, 0 * SCALE, -192 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO1", 224 * SCALE, -240 * SCALE, RIGHT);
    pin ("CO0", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CS", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("CI1", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("CI0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FACS2" */
  symbol ("FACS2P") {
    set_minimum_boundary (0 * SCALE, -272 * SCALE, 224 * SCALE, -16 * SCALE);
    line (224 * SCALE, -240 * SCALE, 208 * SCALE, -240 * SCALE);
    circle (200 * SCALE, -240 * SCALE, 8 * SCALE);
    line (224 * SCALE, -144 * SCALE, 208 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -272 * SCALE, 32 * SCALE, -16 * SCALE);
    line (32 * SCALE, -16 * SCALE, 192 * SCALE, -16 * SCALE);
    line (192 * SCALE, -16 * SCALE, 192 * SCALE, -272 * SCALE);
    line (192 * SCALE, -272 * SCALE, 32 * SCALE, -272 * SCALE);
    line (32 * SCALE, -240 * SCALE, 0 * SCALE, -240 * SCALE);
    line (32 * SCALE, -192 * SCALE, 0 * SCALE, -192 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO1", 224 * SCALE, -240 * SCALE, RIGHT);
    pin ("CO0", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CS", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("CI1", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("CI0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FACS2P" */
  symbol ("FACS2S") {
    set_minimum_boundary (0 * SCALE, -272 * SCALE, 224 * SCALE, -16 * SCALE);
    line (224 * SCALE, -240 * SCALE, 208 * SCALE, -240 * SCALE);
    circle (200 * SCALE, -240 * SCALE, 8 * SCALE);
    line (224 * SCALE, -144 * SCALE, 208 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (32 * SCALE, -272 * SCALE, 32 * SCALE, -16 * SCALE);
    line (32 * SCALE, -16 * SCALE, 192 * SCALE, -16 * SCALE);
    line (192 * SCALE, -16 * SCALE, 192 * SCALE, -272 * SCALE);
    line (192 * SCALE, -272 * SCALE, 32 * SCALE, -272 * SCALE);
    line (32 * SCALE, -240 * SCALE, 0 * SCALE, -240 * SCALE);
    line (32 * SCALE, -192 * SCALE, 0 * SCALE, -192 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("CO1", 224 * SCALE, -240 * SCALE, RIGHT);
    pin ("CO0", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("CS", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("CI1", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("CI0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "FACS2S" */
  symbol ("GCKETF") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    line (112 * SCALE, -16 * SCALE, 112 * SCALE, -32 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TE", 112 * SCALE, 0 * SCALE, UP);
    pin ("E", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "GCKETF" */
  symbol ("GCKETN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    line (112 * SCALE, -16 * SCALE, 112 * SCALE, -32 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TE", 112 * SCALE, 0 * SCALE, UP);
    pin ("E", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "GCKETN" */
  symbol ("GCKETP") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    line (112 * SCALE, -16 * SCALE, 112 * SCALE, -32 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TE", 112 * SCALE, 0 * SCALE, UP);
    pin ("E", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "GCKETP" */
  symbol ("GCKETT") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    line (112 * SCALE, -16 * SCALE, 112 * SCALE, -32 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TE", 112 * SCALE, 0 * SCALE, UP);
    pin ("E", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "GCKETT" */
  symbol ("HA1") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA1" */
  symbol ("HA1P") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA1P" */
  symbol ("HA1S") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA1S" */
  symbol ("HA1T") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA1T" */
  symbol ("HA2") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (16 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA2" */
  symbol ("HA2P") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (16 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA2P" */
  symbol ("HA2T") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (16 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA2T" */
  symbol ("HA3") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (224 * SCALE, -144 * SCALE, 208 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA3" */
  symbol ("HA3P") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (224 * SCALE, -144 * SCALE, 208 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA3P" */
  symbol ("HA3T") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (224 * SCALE, -144 * SCALE, 208 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("C", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("B", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "HA3T" */
  symbol ("INV1") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV1" */
  symbol ("INV12") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV12" */
  symbol ("INV12CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV12CK" */
  symbol ("INV1CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV1CK" */
  symbol ("INV1S") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV1S" */
  symbol ("INV2") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV2" */
  symbol ("INV2CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV2CK" */
  symbol ("INV3") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV3" */
  symbol ("INV3CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV3CK" */
  symbol ("INV4") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV4" */
  symbol ("INV4CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV4CK" */
  symbol ("INV6") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV6" */
  symbol ("INV6CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV6CK" */
  symbol ("INV8") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV8" */
  symbol ("INV8CK") {
    set_minimum_boundary (0 * SCALE, -64 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -44 * SCALE, 88 * SCALE, -64 * SCALE);
    line (76 * SCALE, -44 * SCALE, 88 * SCALE, -44 * SCALE);
    line (76 * SCALE, -64 * SCALE, 76 * SCALE, -44 * SCALE);
    line (88 * SCALE, -64 * SCALE, 100 * SCALE, -64 * SCALE);
    line (64 * SCALE, -64 * SCALE, 76 * SCALE, -64 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (112 * SCALE, -32 * SCALE, 104 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
  } /* end of symbol "INV8CK" */
  symbol ("INVT1") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (104 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (48 * SCALE, -80 * SCALE, 48 * SCALE, -48 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("E", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "INVT1" */
  symbol ("INVT2") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (104 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    line (48 * SCALE, -80 * SCALE, 48 * SCALE, -48 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("E", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "INVT2" */
  symbol ("INVT4") {
    set_minimum_boundary (0 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (48 * SCALE, -80 * SCALE, 48 * SCALE, -48 * SCALE);
    line (104 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    circle (96 * SCALE, -32 * SCALE, 8 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("I", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("E", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "INVT4" */
  symbol ("JKFN") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (48 * SCALE, -96 * SCALE, 32 * SCALE, -112 * SCALE);
    line (32 * SCALE, -80 * SCALE, 48 * SCALE, -96 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("K", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("J", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "JKFN" */
  symbol ("JKFRBN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (48 * SCALE, -96 * SCALE, 32 * SCALE, -112 * SCALE);
    line (32 * SCALE, -80 * SCALE, 48 * SCALE, -96 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("K", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("J", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "JKFRBN" */
  symbol ("JKFRBP") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (208 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    circle (200 * SCALE, -144 * SCALE, 8 * SCALE);
    line (48 * SCALE, -96 * SCALE, 32 * SCALE, -112 * SCALE);
    line (32 * SCALE, -80 * SCALE, 48 * SCALE, -96 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("QB", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("K", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("J", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "JKFRBP" */
  symbol ("JKZN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("K", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("J", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "JKZN" */
  symbol ("JKZRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, 0 * SCALE);
    line (112 * SCALE, -240 * SCALE, 112 * SCALE, -256 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("K", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("J", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "JKZRBN" */
  symbol ("JKZRBP") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, 0 * SCALE);
    line (112 * SCALE, -240 * SCALE, 112 * SCALE, -256 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (208 * SCALE, -176 * SCALE, 224 * SCALE, -176 * SCALE);
    circle (200 * SCALE, -176 * SCALE, 8 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    pin ("QB", 224 * SCALE, -176 * SCALE, RIGHT);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("K", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("J", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "JKZRBP" */
  symbol ("MAO222") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 240 * SCALE, 0 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (224 * SCALE, -96 * SCALE, 240 * SCALE, -96 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (16 * SCALE, -144 * SCALE, 16 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    line (48 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (32 * SCALE, -80 * SCALE, 48 * SCALE, -80 * SCALE);
    line (32 * SCALE, -80 * SCALE, 32 * SCALE, -16 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -96 * SCALE, RIGHT);
    pin ("C1", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "MAO222" */
  symbol ("MAO222P") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 240 * SCALE, 0 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (224 * SCALE, -96 * SCALE, 240 * SCALE, -96 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (16 * SCALE, -144 * SCALE, 16 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    line (48 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (32 * SCALE, -80 * SCALE, 48 * SCALE, -80 * SCALE);
    line (32 * SCALE, -80 * SCALE, 32 * SCALE, -16 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -96 * SCALE, RIGHT);
    pin ("C1", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "MAO222P" */
  symbol ("MAO222S") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 240 * SCALE, 0 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (224 * SCALE, -96 * SCALE, 240 * SCALE, -96 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (16 * SCALE, -144 * SCALE, 16 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    line (48 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (32 * SCALE, -80 * SCALE, 48 * SCALE, -80 * SCALE);
    line (32 * SCALE, -80 * SCALE, 32 * SCALE, -16 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -96 * SCALE, RIGHT);
    pin ("C1", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "MAO222S" */
  symbol ("MAO222T") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 240 * SCALE, 0 * SCALE);
    line (80 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (48 * SCALE, -128 * SCALE, 48 * SCALE, -192 * SCALE);
    line (48 * SCALE, -192 * SCALE, 80 * SCALE, -192 * SCALE);
    line (80 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -64 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -128 * SCALE, 80 * SCALE, -128 * SCALE);
    line (48 * SCALE, -64 * SCALE, 80 * SCALE, -64 * SCALE);
    line (48 * SCALE, 0 * SCALE, 48 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 48 * SCALE, 0 * SCALE);
    line (224 * SCALE, -96 * SCALE, 240 * SCALE, -96 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (16 * SCALE, -144 * SCALE, 16 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    line (48 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (32 * SCALE, -80 * SCALE, 48 * SCALE, -80 * SCALE);
    line (32 * SCALE, -80 * SCALE, 32 * SCALE, -16 * SCALE);
    line (112 * SCALE, -96 * SCALE, 132 * SCALE, -96 * SCALE);
    line (48 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    arc (80 * SCALE, -128 * SCALE, 80 * SCALE, -192 * SCALE,\
        80 * SCALE, -160 * SCALE);
    arc (80 * SCALE, -64 * SCALE, 80 * SCALE, -128 * SCALE,\
        80 * SCALE, -96 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 80 * SCALE, -64 * SCALE,\
        80 * SCALE, -32 * SCALE);
    line (48 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (108 * SCALE, -148 * SCALE, 160 * SCALE, -148 * SCALE);
    arc (224 * SCALE, -96 * SCALE, 160 * SCALE, -148 * SCALE,\
        158.768 * SCALE, -81.099 * SCALE);
    line (108 * SCALE, -44 * SCALE, 160 * SCALE, -44 * SCALE);
    arc (160 * SCALE, -44 * SCALE, 224 * SCALE, -96 * SCALE,\
        158.768 * SCALE, -110.901 * SCALE);
    arc (104 * SCALE, -44 * SCALE, 104 * SCALE, -148 * SCALE,\
        69.7143 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -96 * SCALE, RIGHT);
    pin ("C1", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "MAO222T" */
  symbol ("MAOI1") {
    set_minimum_boundary (-16 * SCALE, -144 * SCALE, 256 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (24 * SCALE, -112 * SCALE, -16 * SCALE, -112 * SCALE);
    line (28 * SCALE, -80 * SCALE, -16 * SCALE, -80 * SCALE);
    line (16 * SCALE, -48 * SCALE, -16 * SCALE, -48 * SCALE);
    line (16 * SCALE, -16 * SCALE, -16 * SCALE, -16 * SCALE);
    line (96 * SCALE, -32 * SCALE, 124 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 64 * SCALE, -64 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (108 * SCALE, -96 * SCALE, 120 * SCALE, -96 * SCALE);
    arc (16 * SCALE, -64 * SCALE, 16 * SCALE, -128 * SCALE,\
        -8 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    circle (100 * SCALE, -96 * SCALE, 8 * SCALE);
    arc (96 * SCALE, -96 * SCALE, 48 * SCALE, -128 * SCALE,\
        52.3784 * SCALE, -82.5676 * SCALE);
    arc (48 * SCALE, -64 * SCALE, 96 * SCALE, -96 * SCALE,\
        52.3784 * SCALE, -109.432 * SCALE);
    line (16 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", -16 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", -16 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", -16 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", -16 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "MAOI1" */
  symbol ("MAOI1H") {
    set_minimum_boundary (-16 * SCALE, -128 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (16 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    arc (48 * SCALE, -64 * SCALE, 96 * SCALE, -96 * SCALE,\
        52.3784 * SCALE, -109.432 * SCALE);
    arc (96 * SCALE, -96 * SCALE, 48 * SCALE, -128 * SCALE,\
        52.3784 * SCALE, -82.5676 * SCALE);
    circle (100 * SCALE, -96 * SCALE, 8 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    arc (16 * SCALE, -64 * SCALE, 16 * SCALE, -128 * SCALE,\
        -8 * SCALE, -96 * SCALE);
    line (108 * SCALE, -96 * SCALE, 120 * SCALE, -96 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 64 * SCALE, -64 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (96 * SCALE, -32 * SCALE, 124 * SCALE, -32 * SCALE);
    line (16 * SCALE, -16 * SCALE, -16 * SCALE, -16 * SCALE);
    line (16 * SCALE, -48 * SCALE, -16 * SCALE, -48 * SCALE);
    line (28 * SCALE, -80 * SCALE, -16 * SCALE, -80 * SCALE);
    line (24 * SCALE, -112 * SCALE, -16 * SCALE, -112 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", -16 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", -16 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", -16 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", -16 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "MAOI1H" */
  symbol ("MAOI1HP") {
    set_minimum_boundary (-16 * SCALE, -128 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (16 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    arc (48 * SCALE, -64 * SCALE, 96 * SCALE, -96 * SCALE,\
        52.3784 * SCALE, -109.432 * SCALE);
    arc (96 * SCALE, -96 * SCALE, 48 * SCALE, -128 * SCALE,\
        52.3784 * SCALE, -82.5676 * SCALE);
    circle (100 * SCALE, -96 * SCALE, 8 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    arc (16 * SCALE, -64 * SCALE, 16 * SCALE, -128 * SCALE,\
        -8 * SCALE, -96 * SCALE);
    line (108 * SCALE, -96 * SCALE, 120 * SCALE, -96 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 64 * SCALE, -64 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (96 * SCALE, -32 * SCALE, 124 * SCALE, -32 * SCALE);
    line (16 * SCALE, -16 * SCALE, -16 * SCALE, -16 * SCALE);
    line (16 * SCALE, -48 * SCALE, -16 * SCALE, -48 * SCALE);
    line (28 * SCALE, -80 * SCALE, -16 * SCALE, -80 * SCALE);
    line (24 * SCALE, -112 * SCALE, -16 * SCALE, -112 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", -16 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", -16 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", -16 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", -16 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "MAOI1HP" */
  symbol ("MAOI1HT") {
    set_minimum_boundary (-16 * SCALE, -128 * SCALE, 256 * SCALE, 0 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (16 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    arc (48 * SCALE, -64 * SCALE, 96 * SCALE, -96 * SCALE,\
        52.3784 * SCALE, -109.432 * SCALE);
    arc (96 * SCALE, -96 * SCALE, 48 * SCALE, -128 * SCALE,\
        52.3784 * SCALE, -82.5676 * SCALE);
    circle (100 * SCALE, -96 * SCALE, 8 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    arc (16 * SCALE, -64 * SCALE, 16 * SCALE, -128 * SCALE,\
        -8 * SCALE, -96 * SCALE);
    line (108 * SCALE, -96 * SCALE, 120 * SCALE, -96 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 64 * SCALE, -64 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (16 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (96 * SCALE, -32 * SCALE, 124 * SCALE, -32 * SCALE);
    line (16 * SCALE, -16 * SCALE, -16 * SCALE, -16 * SCALE);
    line (16 * SCALE, -48 * SCALE, -16 * SCALE, -48 * SCALE);
    line (28 * SCALE, -80 * SCALE, -16 * SCALE, -80 * SCALE);
    line (24 * SCALE, -112 * SCALE, -16 * SCALE, -112 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", -16 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", -16 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", -16 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", -16 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "MAOI1HT" */
  symbol ("MAOI1S") {
    set_minimum_boundary (-16 * SCALE, -128 * SCALE, 256 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (24 * SCALE, -112 * SCALE, -16 * SCALE, -112 * SCALE);
    line (28 * SCALE, -80 * SCALE, -16 * SCALE, -80 * SCALE);
    line (16 * SCALE, -48 * SCALE, -16 * SCALE, -48 * SCALE);
    line (16 * SCALE, -16 * SCALE, -16 * SCALE, -16 * SCALE);
    line (96 * SCALE, -32 * SCALE, 124 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 64 * SCALE, -64 * SCALE,\
        64 * SCALE, -32 * SCALE);
    line (108 * SCALE, -96 * SCALE, 120 * SCALE, -96 * SCALE);
    arc (16 * SCALE, -64 * SCALE, 16 * SCALE, -128 * SCALE,\
        -8 * SCALE, -96 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    circle (100 * SCALE, -96 * SCALE, 8 * SCALE);
    arc (96 * SCALE, -96 * SCALE, 48 * SCALE, -128 * SCALE,\
        52.3784 * SCALE, -82.5676 * SCALE);
    arc (48 * SCALE, -64 * SCALE, 96 * SCALE, -96 * SCALE,\
        52.3784 * SCALE, -109.432 * SCALE);
    line (16 * SCALE, -128 * SCALE, 48 * SCALE, -128 * SCALE);
    line (48 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -64 * SCALE, 48 * SCALE, -64 * SCALE);
    arc (104 * SCALE, -12 * SCALE, 104 * SCALE, -116 * SCALE,\
        69.7143 * SCALE, -64 * SCALE);
    line (240 * SCALE, -64 * SCALE, 256 * SCALE, -64 * SCALE);
    circle (232 * SCALE, -64 * SCALE, 8 * SCALE);
    line (108 * SCALE, -116 * SCALE, 160 * SCALE, -116 * SCALE);
    arc (224 * SCALE, -64 * SCALE, 160 * SCALE, -116 * SCALE,\
        158.768 * SCALE, -49.099 * SCALE);
    line (108 * SCALE, -12 * SCALE, 160 * SCALE, -12 * SCALE);
    arc (160 * SCALE, -12 * SCALE, 224 * SCALE, -64 * SCALE,\
        158.768 * SCALE, -78.901 * SCALE);
    pin ("O", 256 * SCALE, -64 * SCALE, RIGHT);
    pin ("B2", -16 * SCALE, -112 * SCALE, LEFT);
    pin ("B1", -16 * SCALE, -80 * SCALE, LEFT);
    pin ("A2", -16 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", -16 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "MAOI1S" */
  symbol ("MOAI1") {
    set_minimum_boundary (-32 * SCALE, -128 * SCALE, 256 * SCALE, 16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (96 * SCALE, -80 * SCALE, 112 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 112 * SCALE, -16 * SCALE);
    line (-32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (-32 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    circle (88 * SCALE, -80 * SCALE, 8 * SCALE);
    arc (48 * SCALE, -48 * SCALE, 48 * SCALE, -112 * SCALE,\
        48 * SCALE, -80 * SCALE);
    line (0 * SCALE, -112 * SCALE, 48 * SCALE, -112 * SCALE);
    line (0 * SCALE, -48 * SCALE, 48 * SCALE, -48 * SCALE);
    line (0 * SCALE, -48 * SCALE, 0 * SCALE, -112 * SCALE);
    line (8 * SCALE, -32 * SCALE, -32 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -32 * SCALE, 0 * SCALE);
    arc (0 * SCALE, 16 * SCALE, 0 * SCALE, -48 * SCALE,\
        -24 * SCALE, -16 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 80 * SCALE, -16 * SCALE,\
        36.3784 * SCALE, -29.4324 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 32 * SCALE, -48 * SCALE,\
        36.3784 * SCALE, -2.56757 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, 16 * SCALE, 32 * SCALE, 16 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", -32 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", -32 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "MOAI1" */
  symbol ("MOAI1H") {
    set_minimum_boundary (-32 * SCALE, -128 * SCALE, 256 * SCALE, 16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (96 * SCALE, -80 * SCALE, 112 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 112 * SCALE, -16 * SCALE);
    line (-32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (-32 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    circle (88 * SCALE, -80 * SCALE, 8 * SCALE);
    arc (48 * SCALE, -48 * SCALE, 48 * SCALE, -112 * SCALE,\
        48 * SCALE, -80 * SCALE);
    line (0 * SCALE, -112 * SCALE, 48 * SCALE, -112 * SCALE);
    line (0 * SCALE, -48 * SCALE, 48 * SCALE, -48 * SCALE);
    line (0 * SCALE, -48 * SCALE, 0 * SCALE, -112 * SCALE);
    line (8 * SCALE, -32 * SCALE, -32 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -32 * SCALE, 0 * SCALE);
    arc (0 * SCALE, 16 * SCALE, 0 * SCALE, -48 * SCALE,\
        -24 * SCALE, -16 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 80 * SCALE, -16 * SCALE,\
        36.3784 * SCALE, -29.4324 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 32 * SCALE, -48 * SCALE,\
        36.3784 * SCALE, -2.56757 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, 16 * SCALE, 32 * SCALE, 16 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", -32 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", -32 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "MOAI1H" */
  symbol ("MOAI1HP") {
    set_minimum_boundary (-32 * SCALE, -128 * SCALE, 256 * SCALE, 16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (96 * SCALE, -80 * SCALE, 112 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 112 * SCALE, -16 * SCALE);
    line (-32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (-32 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    circle (88 * SCALE, -80 * SCALE, 8 * SCALE);
    arc (48 * SCALE, -48 * SCALE, 48 * SCALE, -112 * SCALE,\
        48 * SCALE, -80 * SCALE);
    line (0 * SCALE, -112 * SCALE, 48 * SCALE, -112 * SCALE);
    line (0 * SCALE, -48 * SCALE, 48 * SCALE, -48 * SCALE);
    line (0 * SCALE, -48 * SCALE, 0 * SCALE, -112 * SCALE);
    line (8 * SCALE, -32 * SCALE, -32 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -32 * SCALE, 0 * SCALE);
    arc (0 * SCALE, 16 * SCALE, 0 * SCALE, -48 * SCALE,\
        -24 * SCALE, -16 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 80 * SCALE, -16 * SCALE,\
        36.3784 * SCALE, -29.4324 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 32 * SCALE, -48 * SCALE,\
        36.3784 * SCALE, -2.56757 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, 16 * SCALE, 32 * SCALE, 16 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", -32 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", -32 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "MOAI1HP" */
  symbol ("MOAI1HT") {
    set_minimum_boundary (-32 * SCALE, -128 * SCALE, 256 * SCALE, 16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (96 * SCALE, -80 * SCALE, 112 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 112 * SCALE, -16 * SCALE);
    line (-32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (-32 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    circle (88 * SCALE, -80 * SCALE, 8 * SCALE);
    arc (48 * SCALE, -48 * SCALE, 48 * SCALE, -112 * SCALE,\
        48 * SCALE, -80 * SCALE);
    line (0 * SCALE, -112 * SCALE, 48 * SCALE, -112 * SCALE);
    line (0 * SCALE, -48 * SCALE, 48 * SCALE, -48 * SCALE);
    line (0 * SCALE, -48 * SCALE, 0 * SCALE, -112 * SCALE);
    line (8 * SCALE, -32 * SCALE, -32 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -32 * SCALE, 0 * SCALE);
    arc (0 * SCALE, 16 * SCALE, 0 * SCALE, -48 * SCALE,\
        -24 * SCALE, -16 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 80 * SCALE, -16 * SCALE,\
        36.3784 * SCALE, -29.4324 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 32 * SCALE, -48 * SCALE,\
        36.3784 * SCALE, -2.56757 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, 16 * SCALE, 32 * SCALE, 16 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", -32 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", -32 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "MOAI1HT" */
  symbol ("MOAI1S") {
    set_minimum_boundary (-32 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (96 * SCALE, -80 * SCALE, 112 * SCALE, -80 * SCALE);
    line (80 * SCALE, -16 * SCALE, 112 * SCALE, -16 * SCALE);
    line (-32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (-32 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    circle (88 * SCALE, -80 * SCALE, 8 * SCALE);
    arc (48 * SCALE, -48 * SCALE, 48 * SCALE, -112 * SCALE,\
        48 * SCALE, -80 * SCALE);
    line (0 * SCALE, -112 * SCALE, 48 * SCALE, -112 * SCALE);
    line (0 * SCALE, -48 * SCALE, 48 * SCALE, -48 * SCALE);
    line (0 * SCALE, -48 * SCALE, 0 * SCALE, -112 * SCALE);
    line (8 * SCALE, -32 * SCALE, -32 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -32 * SCALE, 0 * SCALE);
    arc (0 * SCALE, 16 * SCALE, 0 * SCALE, -48 * SCALE,\
        -24 * SCALE, -16 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 80 * SCALE, -16 * SCALE,\
        36.3784 * SCALE, -29.4324 * SCALE);
    arc (80 * SCALE, -16 * SCALE, 32 * SCALE, -48 * SCALE,\
        36.3784 * SCALE, -2.56757 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, 16 * SCALE, 32 * SCALE, 16 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", -32 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", -32 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "MOAI1S" */
  symbol ("MULBE") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -96 * SCALE, 224 * SCALE, -96 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Z", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("M", 224 * SCALE, -96 * SCALE, RIGHT);
    pin ("M2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("M1", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("M0", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "MULBE" */
  symbol ("MULBEP") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -96 * SCALE, 224 * SCALE, -96 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Z", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("M", 224 * SCALE, -96 * SCALE, RIGHT);
    pin ("M2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("M1", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("M0", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "MULBEP" */
  symbol ("MULBET") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (192 * SCALE, -96 * SCALE, 224 * SCALE, -96 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Z", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("S", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("M", 224 * SCALE, -96 * SCALE, RIGHT);
    pin ("M2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("M1", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("M0", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "MULBET" */
  symbol ("MULPA") {
    set_minimum_boundary (0 * SCALE, -272 * SCALE, 224 * SCALE, -16 * SCALE);
    line (0 * SCALE, -240 * SCALE, 32 * SCALE, -240 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (32 * SCALE, -272 * SCALE, 32 * SCALE, -16 * SCALE);
    line (32 * SCALE, -16 * SCALE, 192 * SCALE, -16 * SCALE);
    line (192 * SCALE, -16 * SCALE, 192 * SCALE, -272 * SCALE);
    line (192 * SCALE, -272 * SCALE, 32 * SCALE, -272 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    pin ("P", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Z", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("S", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("M1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("M0", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("M", 0 * SCALE, -192 * SCALE, LEFT);
  } /* end of symbol "MULPA" */
  symbol ("MULPAP") {
    set_minimum_boundary (0 * SCALE, -272 * SCALE, 224 * SCALE, -16 * SCALE);
    line (0 * SCALE, -240 * SCALE, 32 * SCALE, -240 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (32 * SCALE, -272 * SCALE, 32 * SCALE, -16 * SCALE);
    line (32 * SCALE, -16 * SCALE, 192 * SCALE, -16 * SCALE);
    line (192 * SCALE, -16 * SCALE, 192 * SCALE, -272 * SCALE);
    line (192 * SCALE, -272 * SCALE, 32 * SCALE, -272 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    pin ("P", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Z", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("S", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("M1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("M0", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("M", 0 * SCALE, -192 * SCALE, LEFT);
  } /* end of symbol "MULPAP" */
  symbol ("MULPAT") {
    set_minimum_boundary (0 * SCALE, -272 * SCALE, 224 * SCALE, -16 * SCALE);
    line (0 * SCALE, -240 * SCALE, 32 * SCALE, -240 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (192 * SCALE, -144 * SCALE, 224 * SCALE, -144 * SCALE);
    line (32 * SCALE, -272 * SCALE, 32 * SCALE, -16 * SCALE);
    line (32 * SCALE, -16 * SCALE, 192 * SCALE, -16 * SCALE);
    line (192 * SCALE, -16 * SCALE, 192 * SCALE, -272 * SCALE);
    line (192 * SCALE, -272 * SCALE, 32 * SCALE, -272 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    pin ("P", 224 * SCALE, -144 * SCALE, RIGHT);
    pin ("Z", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("S", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("M1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("M0", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("M", 0 * SCALE, -192 * SCALE, LEFT);
  } /* end of symbol "MULPAT" */
  symbol ("MUX2") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUX2" */
  symbol ("MUX2F") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUX2F" */
  symbol ("MUX2P") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUX2P" */
  symbol ("MUX2S") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUX2S" */
  symbol ("MUX2T") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (224 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUX2T" */
  symbol ("MUX3") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, -32 * SCALE);
    line (224 * SCALE, -160 * SCALE, 256 * SCALE, -160 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 224 * SCALE, -32 * SCALE);
    line (224 * SCALE, -32 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("O", 256 * SCALE, -160 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "MUX3" */
  symbol ("MUX3P") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, -32 * SCALE);
    line (224 * SCALE, -160 * SCALE, 256 * SCALE, -160 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 224 * SCALE, -32 * SCALE);
    line (224 * SCALE, -32 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("O", 256 * SCALE, -160 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "MUX3P" */
  symbol ("MUX3S") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, -32 * SCALE);
    line (224 * SCALE, -160 * SCALE, 256 * SCALE, -160 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 224 * SCALE, -32 * SCALE);
    line (224 * SCALE, -32 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("O", 256 * SCALE, -160 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "MUX3S" */
  symbol ("MUX3T") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, -32 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 224 * SCALE, -32 * SCALE);
    line (224 * SCALE, -32 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (224 * SCALE, -160 * SCALE, 256 * SCALE, -160 * SCALE);
    pin ("O", 256 * SCALE, -160 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -96 * SCALE, LEFT);
  } /* end of symbol "MUX3T" */
  symbol ("MUX4") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -144 * SCALE, 256 * SCALE, -144 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    pin ("O", 256 * SCALE, -144 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -64 * SCALE, LEFT);
  } /* end of symbol "MUX4" */
  symbol ("MUX4P") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -144 * SCALE, 256 * SCALE, -144 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    pin ("O", 256 * SCALE, -144 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -64 * SCALE, LEFT);
  } /* end of symbol "MUX4P" */
  symbol ("MUX4S") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -144 * SCALE, 256 * SCALE, -144 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    pin ("O", 256 * SCALE, -144 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -64 * SCALE, LEFT);
  } /* end of symbol "MUX4S" */
  symbol ("MUX4T") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, 0 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (0 * SCALE, -96 * SCALE, 32 * SCALE, -96 * SCALE);
    line (0 * SCALE, -128 * SCALE, 32 * SCALE, -128 * SCALE);
    line (0 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (0 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (0 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (224 * SCALE, -144 * SCALE, 256 * SCALE, -144 * SCALE);
    pin ("O", 256 * SCALE, -144 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -224 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -192 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -64 * SCALE, LEFT);
  } /* end of symbol "MUX4T" */
  symbol ("MUXB2") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 256 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (0 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    line (224 * SCALE, -96 * SCALE, 256 * SCALE, -96 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -192 * SCALE);
    line (224 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("O", 256 * SCALE, -96 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("EB", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUXB2" */
  symbol ("MUXB2P") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 256 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (0 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    line (224 * SCALE, -96 * SCALE, 256 * SCALE, -96 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -192 * SCALE);
    line (224 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("O", 256 * SCALE, -96 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("EB", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUXB2P" */
  symbol ("MUXB2S") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 256 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (0 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    line (224 * SCALE, -96 * SCALE, 256 * SCALE, -96 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -192 * SCALE);
    line (224 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("O", 256 * SCALE, -96 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("EB", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUXB2S" */
  symbol ("MUXB2T") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 256 * SCALE, 0 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -192 * SCALE);
    line (224 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    line (224 * SCALE, -96 * SCALE, 256 * SCALE, -96 * SCALE);
    line (0 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -96 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("EB", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUXB2T" */
  symbol ("MUXB4") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -144 * SCALE, 256 * SCALE, -144 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    circle (24 * SCALE, -240 * SCALE, 8 * SCALE);
    line (0 * SCALE, -240 * SCALE, 16 * SCALE, -240 * SCALE);
    line (32 * SCALE, -208 * SCALE, 0 * SCALE, -208 * SCALE);
    line (32 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("O", 256 * SCALE, -144 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -208 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("EB", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUXB4" */
  symbol ("MUXB4P") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -144 * SCALE, 256 * SCALE, -144 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    circle (24 * SCALE, -240 * SCALE, 8 * SCALE);
    line (0 * SCALE, -240 * SCALE, 16 * SCALE, -240 * SCALE);
    line (32 * SCALE, -208 * SCALE, 0 * SCALE, -208 * SCALE);
    line (32 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("O", 256 * SCALE, -144 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -208 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("EB", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUXB4P" */
  symbol ("MUXB4S") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, 0 * SCALE);
    line (224 * SCALE, -144 * SCALE, 256 * SCALE, -144 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    circle (24 * SCALE, -240 * SCALE, 8 * SCALE);
    line (0 * SCALE, -240 * SCALE, 16 * SCALE, -240 * SCALE);
    line (32 * SCALE, -208 * SCALE, 0 * SCALE, -208 * SCALE);
    line (32 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("O", 256 * SCALE, -144 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -208 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("EB", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUXB4S" */
  symbol ("MUXB4T") {
    set_minimum_boundary (0 * SCALE, -288 * SCALE, 256 * SCALE, 0 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (32 * SCALE, -208 * SCALE, 0 * SCALE, -208 * SCALE);
    line (0 * SCALE, -240 * SCALE, 16 * SCALE, -240 * SCALE);
    circle (24 * SCALE, -240 * SCALE, 8 * SCALE);
    line (32 * SCALE, -288 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -288 * SCALE);
    line (224 * SCALE, -288 * SCALE, 32 * SCALE, -288 * SCALE);
    line (224 * SCALE, -144 * SCALE, 256 * SCALE, -144 * SCALE);
    pin ("O", 256 * SCALE, -144 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -208 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("EB", 0 * SCALE, -240 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MUXB4T" */
  symbol ("MXL2H") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (256 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    pin ("OB", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MXL2H" */
  symbol ("MXL2HF") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (256 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    pin ("OB", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MXL2HF" */
  symbol ("MXL2HP") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (256 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    pin ("OB", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MXL2HP" */
  symbol ("MXL2HS") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (256 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    pin ("OB", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MXL2HS" */
  symbol ("MXL2HT") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 0 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (256 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 224 * SCALE, 0 * SCALE);
    line (224 * SCALE, 0 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    pin ("OB", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("S", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MXL2HT" */
  symbol ("MXL3") {
    set_minimum_boundary (0 * SCALE, -240 * SCALE, 256 * SCALE, 16 * SCALE);
    circle (232 * SCALE, -112 * SCALE, 8 * SCALE);
    line (256 * SCALE, -112 * SCALE, 240 * SCALE, -112 * SCALE);
    line (32 * SCALE, -240 * SCALE, 32 * SCALE, 16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 224 * SCALE, 16 * SCALE);
    line (224 * SCALE, 16 * SCALE, 224 * SCALE, -240 * SCALE);
    line (224 * SCALE, -240 * SCALE, 32 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("OB", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MXL3" */
  symbol ("MXL3P") {
    set_minimum_boundary (0 * SCALE, -240 * SCALE, 256 * SCALE, 16 * SCALE);
    circle (232 * SCALE, -112 * SCALE, 8 * SCALE);
    line (256 * SCALE, -112 * SCALE, 240 * SCALE, -112 * SCALE);
    line (32 * SCALE, -240 * SCALE, 32 * SCALE, 16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 224 * SCALE, 16 * SCALE);
    line (224 * SCALE, 16 * SCALE, 224 * SCALE, -240 * SCALE);
    line (224 * SCALE, -240 * SCALE, 32 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("OB", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MXL3P" */
  symbol ("MXL3S") {
    set_minimum_boundary (0 * SCALE, -240 * SCALE, 256 * SCALE, 16 * SCALE);
    circle (232 * SCALE, -112 * SCALE, 8 * SCALE);
    line (256 * SCALE, -112 * SCALE, 240 * SCALE, -112 * SCALE);
    line (32 * SCALE, -240 * SCALE, 32 * SCALE, 16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 224 * SCALE, 16 * SCALE);
    line (224 * SCALE, 16 * SCALE, 224 * SCALE, -240 * SCALE);
    line (224 * SCALE, -240 * SCALE, 32 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    pin ("OB", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MXL3S" */
  symbol ("MXL3T") {
    set_minimum_boundary (0 * SCALE, -240 * SCALE, 256 * SCALE, 16 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (32 * SCALE, -176 * SCALE, 0 * SCALE, -176 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -240 * SCALE, 32 * SCALE, 16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 224 * SCALE, 16 * SCALE);
    line (224 * SCALE, 16 * SCALE, 224 * SCALE, -240 * SCALE);
    line (224 * SCALE, -240 * SCALE, 32 * SCALE, -240 * SCALE);
    line (256 * SCALE, -112 * SCALE, 240 * SCALE, -112 * SCALE);
    circle (232 * SCALE, -112 * SCALE, 8 * SCALE);
    pin ("OB", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("S1", 0 * SCALE, -176 * SCALE, LEFT);
    pin ("S0", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("C", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("B", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("A", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "MXL3T" */
  symbol ("ND2") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "ND2" */
  symbol ("ND2F") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "ND2F" */
  symbol ("ND2P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "ND2P" */
  symbol ("ND2S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "ND2S" */
  symbol ("ND2T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "ND2T" */
  symbol ("ND3") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "ND3" */
  symbol ("ND3HT") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "ND3HT" */
  symbol ("ND3P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "ND3P" */
  symbol ("ND3S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -48 * SCALE, 16 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "ND3S" */
  symbol ("ND4") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "ND4" */
  symbol ("ND4P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "ND4P" */
  symbol ("ND4S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "ND4S" */
  symbol ("ND4T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE);
    circle (148 * SCALE, -48 * SCALE, 8 * SCALE);
    line (16 * SCALE, 0 * SCALE, 96 * SCALE, 0 * SCALE);
    arc (92 * SCALE, 0 * SCALE, 92 * SCALE, -96 * SCALE,\
        92 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 96 * SCALE, -96 * SCALE);
    line (0 * SCALE, -64 * SCALE, 16 * SCALE, -64 * SCALE);
    line (16 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (0 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "ND4T" */
  symbol ("NR2") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "NR2" */
  symbol ("NR2F") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "NR2F" */
  symbol ("NR2P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "NR2P" */
  symbol ("NR2T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "NR2T" */
  symbol ("NR3") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -48 * SCALE, 36 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "NR3" */
  symbol ("NR3H") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -48 * SCALE, 36 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "NR3H" */
  symbol ("NR3HP") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -48 * SCALE, 36 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "NR3HP" */
  symbol ("NR3HT") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -48 * SCALE, 36 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "NR3HT" */
  symbol ("NR4") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (88 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    line (88 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 32 * SCALE, -32 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "NR4" */
  symbol ("NR4P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (88 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    line (88 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 32 * SCALE, -32 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "NR4P" */
  symbol ("NR4S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (88 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    line (88 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 32 * SCALE, -32 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "NR4S" */
  symbol ("NR4T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    line (0 * SCALE, -32 * SCALE, 32 * SCALE, -32 * SCALE);
    line (0 * SCALE, -64 * SCALE, 32 * SCALE, -64 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (88 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (88 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "NR4T" */
  symbol ("OA112") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (256 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (112 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (44 * SCALE, 16 * SCALE, 0 * SCALE, 16 * SCALE);
    arc (32 * SCALE, 32 * SCALE, 32 * SCALE, -32 * SCALE,\
        8 * SCALE, -0 * SCALE);
    line (32 * SCALE, -32 * SCALE, 64 * SCALE, -32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 64 * SCALE, 32 * SCALE);
    arc (64 * SCALE, 32 * SCALE, 112 * SCALE, 0 * SCALE,\
        68.3784 * SCALE, -13.4324 * SCALE);
    arc (112 * SCALE, 0 * SCALE, 64 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, 13.4324 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, 16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA112" */
  symbol ("OA112P") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (256 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (112 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (44 * SCALE, 16 * SCALE, 0 * SCALE, 16 * SCALE);
    arc (32 * SCALE, 32 * SCALE, 32 * SCALE, -32 * SCALE,\
        8 * SCALE, -0 * SCALE);
    line (32 * SCALE, -32 * SCALE, 64 * SCALE, -32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 64 * SCALE, 32 * SCALE);
    arc (64 * SCALE, 32 * SCALE, 112 * SCALE, 0 * SCALE,\
        68.3784 * SCALE, -13.4324 * SCALE);
    arc (112 * SCALE, 0 * SCALE, 64 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, 13.4324 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, 16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA112P" */
  symbol ("OA112S") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (256 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (112 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (44 * SCALE, 16 * SCALE, 0 * SCALE, 16 * SCALE);
    arc (32 * SCALE, 32 * SCALE, 32 * SCALE, -32 * SCALE,\
        8 * SCALE, -0 * SCALE);
    line (32 * SCALE, -32 * SCALE, 64 * SCALE, -32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 64 * SCALE, 32 * SCALE);
    arc (64 * SCALE, 32 * SCALE, 112 * SCALE, 0 * SCALE,\
        68.3784 * SCALE, -13.4324 * SCALE);
    arc (112 * SCALE, 0 * SCALE, 64 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, 13.4324 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, 16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA112S" */
  symbol ("OA112T") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (256 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (112 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (44 * SCALE, 16 * SCALE, 0 * SCALE, 16 * SCALE);
    arc (32 * SCALE, 32 * SCALE, 32 * SCALE, -32 * SCALE,\
        8 * SCALE, -0 * SCALE);
    line (32 * SCALE, -32 * SCALE, 64 * SCALE, -32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 64 * SCALE, 32 * SCALE);
    arc (64 * SCALE, 32 * SCALE, 112 * SCALE, 0 * SCALE,\
        68.3784 * SCALE, -13.4324 * SCALE);
    arc (112 * SCALE, 0 * SCALE, 64 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, 13.4324 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, 16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA112T" */
  symbol ("OA12") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -48 * SCALE, 240 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (44 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    arc (32 * SCALE, 0 * SCALE, 32 * SCALE, -64 * SCALE,\
        8 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 112 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, -45.4324 * SCALE);
    arc (112 * SCALE, -32 * SCALE, 64 * SCALE, -64 * SCALE,\
        68.3784 * SCALE, -18.5676 * SCALE);
    line (32 * SCALE, -64 * SCALE, 64 * SCALE, -64 * SCALE);
    line (32 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA12" */
  symbol ("OA12P") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -48 * SCALE, 240 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (44 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    arc (32 * SCALE, 0 * SCALE, 32 * SCALE, -64 * SCALE,\
        8 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 112 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, -45.4324 * SCALE);
    arc (112 * SCALE, -32 * SCALE, 64 * SCALE, -64 * SCALE,\
        68.3784 * SCALE, -18.5676 * SCALE);
    line (32 * SCALE, -64 * SCALE, 64 * SCALE, -64 * SCALE);
    line (32 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA12P" */
  symbol ("OA12S") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 240 * SCALE, 16 * SCALE);
    line (224 * SCALE, -48 * SCALE, 240 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (44 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    arc (32 * SCALE, 0 * SCALE, 32 * SCALE, -64 * SCALE,\
        8 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 112 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, -45.4324 * SCALE);
    arc (112 * SCALE, -32 * SCALE, 64 * SCALE, -64 * SCALE,\
        68.3784 * SCALE, -18.5676 * SCALE);
    line (32 * SCALE, -64 * SCALE, 64 * SCALE, -64 * SCALE);
    line (32 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA12S" */
  symbol ("OA12T") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 240 * SCALE, 0 * SCALE);
    line (224 * SCALE, -48 * SCALE, 240 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (44 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    arc (32 * SCALE, 0 * SCALE, 32 * SCALE, -64 * SCALE,\
        8 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 112 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, -45.4324 * SCALE);
    arc (112 * SCALE, -32 * SCALE, 64 * SCALE, -64 * SCALE,\
        68.3784 * SCALE, -18.5676 * SCALE);
    line (32 * SCALE, -64 * SCALE, 64 * SCALE, -64 * SCALE);
    line (32 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA12T" */
  symbol ("OA13") {
    set_minimum_boundary (-32 * SCALE, -112 * SCALE, 256 * SCALE, 48 * SCALE);
    line (256 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (-32 * SCALE, -48 * SCALE, 12 * SCALE, -48 * SCALE);
    line (20 * SCALE, -16 * SCALE, -32 * SCALE, -16 * SCALE);
    line (-28 * SCALE, 16 * SCALE, -32 * SCALE, 16 * SCALE);
    line (8 * SCALE, 16 * SCALE, -28 * SCALE, 16 * SCALE);
    arc (-8 * SCALE, 36 * SCALE, -8 * SCALE, -68 * SCALE,\
        -42.2857 * SCALE, -16 * SCALE);
    line (-4 * SCALE, -68 * SCALE, 48 * SCALE, -68 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 48 * SCALE, -68 * SCALE,\
        46.7679 * SCALE, -1.09898 * SCALE);
    line (-4 * SCALE, 36 * SCALE, 48 * SCALE, 36 * SCALE);
    arc (48 * SCALE, 36 * SCALE, 112 * SCALE, -16 * SCALE,\
        46.7679 * SCALE, -30.901 * SCALE);
    line (112 * SCALE, -80 * SCALE, -32 * SCALE, -80 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B3", -32 * SCALE, -48 * SCALE, LEFT);
    pin ("B2", -32 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, 16 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA13" */
  symbol ("OA13P") {
    set_minimum_boundary (-32 * SCALE, -112 * SCALE, 256 * SCALE, 48 * SCALE);
    line (256 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (-32 * SCALE, -48 * SCALE, 12 * SCALE, -48 * SCALE);
    line (20 * SCALE, -16 * SCALE, -32 * SCALE, -16 * SCALE);
    line (-28 * SCALE, 16 * SCALE, -32 * SCALE, 16 * SCALE);
    line (8 * SCALE, 16 * SCALE, -28 * SCALE, 16 * SCALE);
    arc (-8 * SCALE, 36 * SCALE, -8 * SCALE, -68 * SCALE,\
        -42.2857 * SCALE, -16 * SCALE);
    line (-4 * SCALE, -68 * SCALE, 48 * SCALE, -68 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 48 * SCALE, -68 * SCALE,\
        46.7679 * SCALE, -1.09898 * SCALE);
    line (-4 * SCALE, 36 * SCALE, 48 * SCALE, 36 * SCALE);
    arc (48 * SCALE, 36 * SCALE, 112 * SCALE, -16 * SCALE,\
        46.7679 * SCALE, -30.901 * SCALE);
    line (112 * SCALE, -80 * SCALE, -32 * SCALE, -80 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B3", -32 * SCALE, -48 * SCALE, LEFT);
    pin ("B2", -32 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, 16 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA13P" */
  symbol ("OA13S") {
    set_minimum_boundary (-32 * SCALE, -112 * SCALE, 256 * SCALE, 48 * SCALE);
    line (256 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (-32 * SCALE, -48 * SCALE, 12 * SCALE, -48 * SCALE);
    line (20 * SCALE, -16 * SCALE, -32 * SCALE, -16 * SCALE);
    line (-28 * SCALE, 16 * SCALE, -32 * SCALE, 16 * SCALE);
    line (8 * SCALE, 16 * SCALE, -28 * SCALE, 16 * SCALE);
    arc (-8 * SCALE, 36 * SCALE, -8 * SCALE, -68 * SCALE,\
        -42.2857 * SCALE, -16 * SCALE);
    line (-4 * SCALE, -68 * SCALE, 48 * SCALE, -68 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 48 * SCALE, -68 * SCALE,\
        46.7679 * SCALE, -1.09898 * SCALE);
    line (-4 * SCALE, 36 * SCALE, 48 * SCALE, 36 * SCALE);
    arc (48 * SCALE, 36 * SCALE, 112 * SCALE, -16 * SCALE,\
        46.7679 * SCALE, -30.901 * SCALE);
    line (112 * SCALE, -80 * SCALE, -32 * SCALE, -80 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B3", -32 * SCALE, -48 * SCALE, LEFT);
    pin ("B2", -32 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, 16 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA13S" */
  symbol ("OA13T") {
    set_minimum_boundary (-32 * SCALE, -112 * SCALE, 256 * SCALE, 48 * SCALE);
    line (256 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (-32 * SCALE, -48 * SCALE, 12 * SCALE, -48 * SCALE);
    line (20 * SCALE, -16 * SCALE, -32 * SCALE, -16 * SCALE);
    line (-28 * SCALE, 16 * SCALE, -32 * SCALE, 16 * SCALE);
    line (8 * SCALE, 16 * SCALE, -28 * SCALE, 16 * SCALE);
    arc (-8 * SCALE, 36 * SCALE, -8 * SCALE, -68 * SCALE,\
        -42.2857 * SCALE, -16 * SCALE);
    line (-4 * SCALE, -68 * SCALE, 48 * SCALE, -68 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 48 * SCALE, -68 * SCALE,\
        46.7679 * SCALE, -1.09898 * SCALE);
    line (-4 * SCALE, 36 * SCALE, 48 * SCALE, 36 * SCALE);
    arc (48 * SCALE, 36 * SCALE, 112 * SCALE, -16 * SCALE,\
        46.7679 * SCALE, -30.901 * SCALE);
    line (112 * SCALE, -80 * SCALE, -32 * SCALE, -80 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B3", -32 * SCALE, -48 * SCALE, LEFT);
    pin ("B2", -32 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, 16 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OA13T" */
  symbol ("OA22") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 240 * SCALE, 16 * SCALE);
    line (224 * SCALE, -48 * SCALE, 240 * SCALE, -48 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OA22" */
  symbol ("OA222") {
    set_minimum_boundary (0 * SCALE, -176 * SCALE, 240 * SCALE, 16 * SCALE);
    line (44 * SCALE, -128 * SCALE, 0 * SCALE, -128 * SCALE);
    line (112 * SCALE, -128 * SCALE, 112 * SCALE, -144 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -16 * SCALE);
    line (40 * SCALE, -160 * SCALE, 0 * SCALE, -160 * SCALE);
    arc (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE,\
        8 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 64 * SCALE, -176 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    arc (64 * SCALE, -112 * SCALE, 112 * SCALE, -144 * SCALE,\
        68.3784 * SCALE, -157.432 * SCALE);
    arc (112 * SCALE, -144 * SCALE, 64 * SCALE, -176 * SCALE,\
        68.3784 * SCALE, -130.568 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -128 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    line (224 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    arc (176 * SCALE, -32 * SCALE, 176 * SCALE, -128 * SCALE,\
        176 * SCALE, -80 * SCALE);
    line (176 * SCALE, -128 * SCALE, 112 * SCALE, -128 * SCALE);
    line (176 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    pin ("O", 240 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OA222" */
  symbol ("OA222P") {
    set_minimum_boundary (0 * SCALE, -176 * SCALE, 240 * SCALE, 16 * SCALE);
    line (44 * SCALE, -128 * SCALE, 0 * SCALE, -128 * SCALE);
    line (112 * SCALE, -128 * SCALE, 112 * SCALE, -144 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -16 * SCALE);
    line (40 * SCALE, -160 * SCALE, 0 * SCALE, -160 * SCALE);
    arc (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE,\
        8 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 64 * SCALE, -176 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    arc (64 * SCALE, -112 * SCALE, 112 * SCALE, -144 * SCALE,\
        68.3784 * SCALE, -157.432 * SCALE);
    arc (112 * SCALE, -144 * SCALE, 64 * SCALE, -176 * SCALE,\
        68.3784 * SCALE, -130.568 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -128 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    line (224 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    arc (176 * SCALE, -32 * SCALE, 176 * SCALE, -128 * SCALE,\
        176 * SCALE, -80 * SCALE);
    line (176 * SCALE, -128 * SCALE, 112 * SCALE, -128 * SCALE);
    line (176 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    pin ("O", 240 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OA222P" */
  symbol ("OA222S") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 240 * SCALE, 32 * SCALE);
    line (44 * SCALE, -128 * SCALE, 0 * SCALE, -128 * SCALE);
    line (112 * SCALE, -128 * SCALE, 112 * SCALE, -144 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -16 * SCALE);
    line (40 * SCALE, -160 * SCALE, 0 * SCALE, -160 * SCALE);
    arc (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE,\
        8 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 64 * SCALE, -176 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    arc (64 * SCALE, -112 * SCALE, 112 * SCALE, -144 * SCALE,\
        68.3784 * SCALE, -157.432 * SCALE);
    arc (112 * SCALE, -144 * SCALE, 64 * SCALE, -176 * SCALE,\
        68.3784 * SCALE, -130.568 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -128 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    line (224 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    arc (176 * SCALE, -32 * SCALE, 176 * SCALE, -128 * SCALE,\
        176 * SCALE, -80 * SCALE);
    line (176 * SCALE, -128 * SCALE, 112 * SCALE, -128 * SCALE);
    line (176 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    pin ("O", 240 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OA222S" */
  symbol ("OA222T") {
    set_minimum_boundary (0 * SCALE, -176 * SCALE, 240 * SCALE, 16 * SCALE);
    line (44 * SCALE, -128 * SCALE, 0 * SCALE, -128 * SCALE);
    line (112 * SCALE, -128 * SCALE, 112 * SCALE, -144 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -16 * SCALE);
    line (40 * SCALE, -160 * SCALE, 0 * SCALE, -160 * SCALE);
    arc (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE,\
        8 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 64 * SCALE, -176 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    arc (64 * SCALE, -112 * SCALE, 112 * SCALE, -144 * SCALE,\
        68.3784 * SCALE, -157.432 * SCALE);
    arc (112 * SCALE, -144 * SCALE, 64 * SCALE, -176 * SCALE,\
        68.3784 * SCALE, -130.568 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -128 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    line (224 * SCALE, -80 * SCALE, 240 * SCALE, -80 * SCALE);
    arc (176 * SCALE, -32 * SCALE, 176 * SCALE, -128 * SCALE,\
        176 * SCALE, -80 * SCALE);
    line (176 * SCALE, -128 * SCALE, 112 * SCALE, -128 * SCALE);
    line (176 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    pin ("O", 240 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OA222T" */
  symbol ("OA22P") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 240 * SCALE, 16 * SCALE);
    line (224 * SCALE, -48 * SCALE, 240 * SCALE, -48 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OA22P" */
  symbol ("OA22S") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 240 * SCALE, 32 * SCALE);
    line (224 * SCALE, -48 * SCALE, 240 * SCALE, -48 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OA22S" */
  symbol ("OA22T") {
    set_minimum_boundary (0 * SCALE, -112 * SCALE, 240 * SCALE, 16 * SCALE);
    line (224 * SCALE, -48 * SCALE, 240 * SCALE, -48 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    pin ("O", 240 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OA22T" */
  symbol ("OAI112H") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (112 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (44 * SCALE, 16 * SCALE, 0 * SCALE, 16 * SCALE);
    arc (32 * SCALE, 32 * SCALE, 32 * SCALE, -32 * SCALE,\
        8 * SCALE, -0 * SCALE);
    line (32 * SCALE, -32 * SCALE, 64 * SCALE, -32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 64 * SCALE, 32 * SCALE);
    arc (64 * SCALE, 32 * SCALE, 112 * SCALE, 0 * SCALE,\
        68.3784 * SCALE, -13.4324 * SCALE);
    arc (112 * SCALE, 0 * SCALE, 64 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, 13.4324 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, 16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI112H" */
  symbol ("OAI112HP") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (112 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (44 * SCALE, 16 * SCALE, 0 * SCALE, 16 * SCALE);
    arc (32 * SCALE, 32 * SCALE, 32 * SCALE, -32 * SCALE,\
        8 * SCALE, -0 * SCALE);
    line (32 * SCALE, -32 * SCALE, 64 * SCALE, -32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 64 * SCALE, 32 * SCALE);
    arc (64 * SCALE, 32 * SCALE, 112 * SCALE, 0 * SCALE,\
        68.3784 * SCALE, -13.4324 * SCALE);
    arc (112 * SCALE, 0 * SCALE, 64 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, 13.4324 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, 16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI112HP" */
  symbol ("OAI112HS") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (112 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (44 * SCALE, 16 * SCALE, 0 * SCALE, 16 * SCALE);
    arc (32 * SCALE, 32 * SCALE, 32 * SCALE, -32 * SCALE,\
        8 * SCALE, -0 * SCALE);
    line (32 * SCALE, -32 * SCALE, 64 * SCALE, -32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 64 * SCALE, 32 * SCALE);
    arc (64 * SCALE, 32 * SCALE, 112 * SCALE, 0 * SCALE,\
        68.3784 * SCALE, -13.4324 * SCALE);
    arc (112 * SCALE, 0 * SCALE, 64 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, 13.4324 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, 16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI112HS" */
  symbol ("OAI112HT") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (112 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (44 * SCALE, 16 * SCALE, 0 * SCALE, 16 * SCALE);
    arc (32 * SCALE, 32 * SCALE, 32 * SCALE, -32 * SCALE,\
        8 * SCALE, -0 * SCALE);
    line (32 * SCALE, -32 * SCALE, 64 * SCALE, -32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 64 * SCALE, 32 * SCALE);
    arc (64 * SCALE, 32 * SCALE, 112 * SCALE, 0 * SCALE,\
        68.3784 * SCALE, -13.4324 * SCALE);
    arc (112 * SCALE, 0 * SCALE, 64 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, 13.4324 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, 16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI112HT" */
  symbol ("OAI12H") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (44 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    arc (32 * SCALE, 0 * SCALE, 32 * SCALE, -64 * SCALE,\
        8 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 112 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, -45.4324 * SCALE);
    arc (112 * SCALE, -32 * SCALE, 64 * SCALE, -64 * SCALE,\
        68.3784 * SCALE, -18.5676 * SCALE);
    line (32 * SCALE, -64 * SCALE, 64 * SCALE, -64 * SCALE);
    line (32 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI12H" */
  symbol ("OAI12HP") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (44 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    arc (32 * SCALE, 0 * SCALE, 32 * SCALE, -64 * SCALE,\
        8 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 112 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, -45.4324 * SCALE);
    arc (112 * SCALE, -32 * SCALE, 64 * SCALE, -64 * SCALE,\
        68.3784 * SCALE, -18.5676 * SCALE);
    line (32 * SCALE, -64 * SCALE, 64 * SCALE, -64 * SCALE);
    line (32 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI12HP" */
  symbol ("OAI12HS") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (44 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    arc (32 * SCALE, 0 * SCALE, 32 * SCALE, -64 * SCALE,\
        8 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 112 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, -45.4324 * SCALE);
    arc (112 * SCALE, -32 * SCALE, 64 * SCALE, -64 * SCALE,\
        68.3784 * SCALE, -18.5676 * SCALE);
    line (32 * SCALE, -64 * SCALE, 64 * SCALE, -64 * SCALE);
    line (32 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI12HS" */
  symbol ("OAI12HT") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 0 * SCALE);
    line (112 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (40 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (44 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    arc (32 * SCALE, 0 * SCALE, 32 * SCALE, -64 * SCALE,\
        8 * SCALE, -32 * SCALE);
    arc (64 * SCALE, 0 * SCALE, 112 * SCALE, -32 * SCALE,\
        68.3784 * SCALE, -45.4324 * SCALE);
    arc (112 * SCALE, -32 * SCALE, 64 * SCALE, -64 * SCALE,\
        68.3784 * SCALE, -18.5676 * SCALE);
    line (32 * SCALE, -64 * SCALE, 64 * SCALE, -64 * SCALE);
    line (32 * SCALE, 0 * SCALE, 64 * SCALE, 0 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI12HT" */
  symbol ("OAI13H") {
    set_minimum_boundary (-32 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (-32 * SCALE, -48 * SCALE, 12 * SCALE, -48 * SCALE);
    line (20 * SCALE, -16 * SCALE, -32 * SCALE, -16 * SCALE);
    line (-28 * SCALE, 16 * SCALE, -32 * SCALE, 16 * SCALE);
    line (8 * SCALE, 16 * SCALE, -28 * SCALE, 16 * SCALE);
    arc (-8 * SCALE, 36 * SCALE, -8 * SCALE, -68 * SCALE,\
        -42.2857 * SCALE, -16 * SCALE);
    line (-4 * SCALE, -68 * SCALE, 48 * SCALE, -68 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 48 * SCALE, -68 * SCALE,\
        46.7679 * SCALE, -1.09898 * SCALE);
    line (-4 * SCALE, 36 * SCALE, 48 * SCALE, 36 * SCALE);
    arc (48 * SCALE, 36 * SCALE, 112 * SCALE, -16 * SCALE,\
        46.7679 * SCALE, -30.901 * SCALE);
    line (112 * SCALE, -80 * SCALE, -32 * SCALE, -80 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B3", -32 * SCALE, -48 * SCALE, LEFT);
    pin ("B2", -32 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, 16 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI13H" */
  symbol ("OAI13HP") {
    set_minimum_boundary (-32 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (-32 * SCALE, -48 * SCALE, 12 * SCALE, -48 * SCALE);
    line (20 * SCALE, -16 * SCALE, -32 * SCALE, -16 * SCALE);
    line (-28 * SCALE, 16 * SCALE, -32 * SCALE, 16 * SCALE);
    line (8 * SCALE, 16 * SCALE, -28 * SCALE, 16 * SCALE);
    arc (-8 * SCALE, 36 * SCALE, -8 * SCALE, -68 * SCALE,\
        -42.2857 * SCALE, -16 * SCALE);
    line (-4 * SCALE, -68 * SCALE, 48 * SCALE, -68 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 48 * SCALE, -68 * SCALE,\
        46.7679 * SCALE, -1.09898 * SCALE);
    line (-4 * SCALE, 36 * SCALE, 48 * SCALE, 36 * SCALE);
    arc (48 * SCALE, 36 * SCALE, 112 * SCALE, -16 * SCALE,\
        46.7679 * SCALE, -30.901 * SCALE);
    line (112 * SCALE, -80 * SCALE, -32 * SCALE, -80 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B3", -32 * SCALE, -48 * SCALE, LEFT);
    pin ("B2", -32 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, 16 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI13HP" */
  symbol ("OAI13HS") {
    set_minimum_boundary (-32 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (-32 * SCALE, -48 * SCALE, 12 * SCALE, -48 * SCALE);
    line (20 * SCALE, -16 * SCALE, -32 * SCALE, -16 * SCALE);
    line (-28 * SCALE, 16 * SCALE, -32 * SCALE, 16 * SCALE);
    line (8 * SCALE, 16 * SCALE, -28 * SCALE, 16 * SCALE);
    arc (-8 * SCALE, 36 * SCALE, -8 * SCALE, -68 * SCALE,\
        -42.2857 * SCALE, -16 * SCALE);
    line (-4 * SCALE, -68 * SCALE, 48 * SCALE, -68 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 48 * SCALE, -68 * SCALE,\
        46.7679 * SCALE, -1.09898 * SCALE);
    line (-4 * SCALE, 36 * SCALE, 48 * SCALE, 36 * SCALE);
    arc (48 * SCALE, 36 * SCALE, 112 * SCALE, -16 * SCALE,\
        46.7679 * SCALE, -30.901 * SCALE);
    line (112 * SCALE, -80 * SCALE, -32 * SCALE, -80 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B3", -32 * SCALE, -48 * SCALE, LEFT);
    pin ("B2", -32 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, 16 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI13HS" */
  symbol ("OAI13HT") {
    set_minimum_boundary (-32 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (-32 * SCALE, -48 * SCALE, 12 * SCALE, -48 * SCALE);
    line (20 * SCALE, -16 * SCALE, -32 * SCALE, -16 * SCALE);
    line (-28 * SCALE, 16 * SCALE, -32 * SCALE, 16 * SCALE);
    line (8 * SCALE, 16 * SCALE, -28 * SCALE, 16 * SCALE);
    arc (-8 * SCALE, 36 * SCALE, -8 * SCALE, -68 * SCALE,\
        -42.2857 * SCALE, -16 * SCALE);
    line (-4 * SCALE, -68 * SCALE, 48 * SCALE, -68 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 48 * SCALE, -68 * SCALE,\
        46.7679 * SCALE, -1.09898 * SCALE);
    line (-4 * SCALE, 36 * SCALE, 48 * SCALE, 36 * SCALE);
    arc (48 * SCALE, 36 * SCALE, 112 * SCALE, -16 * SCALE,\
        46.7679 * SCALE, -30.901 * SCALE);
    line (112 * SCALE, -80 * SCALE, -32 * SCALE, -80 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B3", -32 * SCALE, -48 * SCALE, LEFT);
    pin ("B2", -32 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", -32 * SCALE, 16 * SCALE, LEFT);
    pin ("A1", -32 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OAI13HT" */
  symbol ("OAI222H") {
    set_minimum_boundary (0 * SCALE, -176 * SCALE, 256 * SCALE, 16 * SCALE);
    line (240 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (44 * SCALE, -128 * SCALE, 0 * SCALE, -128 * SCALE);
    line (112 * SCALE, -128 * SCALE, 112 * SCALE, -144 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -16 * SCALE);
    line (40 * SCALE, -160 * SCALE, 0 * SCALE, -160 * SCALE);
    arc (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE,\
        8 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 64 * SCALE, -176 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    arc (64 * SCALE, -112 * SCALE, 112 * SCALE, -144 * SCALE,\
        68.3784 * SCALE, -157.432 * SCALE);
    arc (112 * SCALE, -144 * SCALE, 64 * SCALE, -176 * SCALE,\
        68.3784 * SCALE, -130.568 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -128 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, -32 * SCALE, 176 * SCALE, -128 * SCALE,\
        176 * SCALE, -80 * SCALE);
    line (176 * SCALE, -128 * SCALE, 112 * SCALE, -128 * SCALE);
    line (176 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OAI222H" */
  symbol ("OAI222HP") {
    set_minimum_boundary (0 * SCALE, -176 * SCALE, 256 * SCALE, 16 * SCALE);
    line (240 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (44 * SCALE, -128 * SCALE, 0 * SCALE, -128 * SCALE);
    line (112 * SCALE, -128 * SCALE, 112 * SCALE, -144 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -16 * SCALE);
    line (40 * SCALE, -160 * SCALE, 0 * SCALE, -160 * SCALE);
    arc (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE,\
        8 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 64 * SCALE, -176 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    arc (64 * SCALE, -112 * SCALE, 112 * SCALE, -144 * SCALE,\
        68.3784 * SCALE, -157.432 * SCALE);
    arc (112 * SCALE, -144 * SCALE, 64 * SCALE, -176 * SCALE,\
        68.3784 * SCALE, -130.568 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -128 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, -32 * SCALE, 176 * SCALE, -128 * SCALE,\
        176 * SCALE, -80 * SCALE);
    line (176 * SCALE, -128 * SCALE, 112 * SCALE, -128 * SCALE);
    line (176 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OAI222HP" */
  symbol ("OAI222HT") {
    set_minimum_boundary (0 * SCALE, -176 * SCALE, 256 * SCALE, 16 * SCALE);
    line (240 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (44 * SCALE, -128 * SCALE, 0 * SCALE, -128 * SCALE);
    line (112 * SCALE, -128 * SCALE, 112 * SCALE, -144 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -16 * SCALE);
    line (40 * SCALE, -160 * SCALE, 0 * SCALE, -160 * SCALE);
    arc (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE,\
        8 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 64 * SCALE, -176 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    arc (64 * SCALE, -112 * SCALE, 112 * SCALE, -144 * SCALE,\
        68.3784 * SCALE, -157.432 * SCALE);
    arc (112 * SCALE, -144 * SCALE, 64 * SCALE, -176 * SCALE,\
        68.3784 * SCALE, -130.568 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -128 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, -32 * SCALE, 176 * SCALE, -128 * SCALE,\
        176 * SCALE, -80 * SCALE);
    line (176 * SCALE, -128 * SCALE, 112 * SCALE, -128 * SCALE);
    line (176 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OAI222HT" */
  symbol ("OAI222S") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 256 * SCALE, 32 * SCALE);
    line (240 * SCALE, -80 * SCALE, 256 * SCALE, -80 * SCALE);
    circle (232 * SCALE, -80 * SCALE, 8 * SCALE);
    line (44 * SCALE, -128 * SCALE, 0 * SCALE, -128 * SCALE);
    line (112 * SCALE, -128 * SCALE, 112 * SCALE, -144 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -16 * SCALE);
    line (40 * SCALE, -160 * SCALE, 0 * SCALE, -160 * SCALE);
    arc (32 * SCALE, -112 * SCALE, 32 * SCALE, -176 * SCALE,\
        8 * SCALE, -144 * SCALE);
    line (32 * SCALE, -176 * SCALE, 64 * SCALE, -176 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    arc (64 * SCALE, -112 * SCALE, 112 * SCALE, -144 * SCALE,\
        68.3784 * SCALE, -157.432 * SCALE);
    arc (112 * SCALE, -144 * SCALE, 64 * SCALE, -176 * SCALE,\
        68.3784 * SCALE, -130.568 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (112 * SCALE, -32 * SCALE, 112 * SCALE, -128 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, -32 * SCALE, 176 * SCALE, -128 * SCALE,\
        176 * SCALE, -80 * SCALE);
    line (176 * SCALE, -128 * SCALE, 112 * SCALE, -128 * SCALE);
    line (176 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    pin ("O", 256 * SCALE, -80 * SCALE, RIGHT);
    pin ("C2", 0 * SCALE, -160 * SCALE, LEFT);
    pin ("C1", 0 * SCALE, -128 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OAI222S" */
  symbol ("OAI22H") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 16 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OAI22H" */
  symbol ("OAI22HP") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 16 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OAI22HP" */
  symbol ("OAI22HT") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 16 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OAI22HT" */
  symbol ("OAI22S") {
    set_minimum_boundary (0 * SCALE, -128 * SCALE, 256 * SCALE, 32 * SCALE);
    line (40 * SCALE, -96 * SCALE, 0 * SCALE, -96 * SCALE);
    line (44 * SCALE, -64 * SCALE, 0 * SCALE, -64 * SCALE);
    arc (32 * SCALE, -48 * SCALE, 32 * SCALE, -112 * SCALE,\
        8 * SCALE, -80 * SCALE);
    line (32 * SCALE, -112 * SCALE, 64 * SCALE, -112 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    arc (64 * SCALE, -48 * SCALE, 112 * SCALE, -80 * SCALE,\
        68.3784 * SCALE, -93.4324 * SCALE);
    arc (112 * SCALE, -80 * SCALE, 64 * SCALE, -112 * SCALE,\
        68.3784 * SCALE, -66.5676 * SCALE);
    line (40 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (44 * SCALE, 0 * SCALE, 0 * SCALE, 0 * SCALE);
    arc (32 * SCALE, 16 * SCALE, 32 * SCALE, -48 * SCALE,\
        8 * SCALE, -16 * SCALE);
    line (32 * SCALE, -48 * SCALE, 64 * SCALE, -48 * SCALE);
    line (32 * SCALE, 16 * SCALE, 64 * SCALE, 16 * SCALE);
    arc (64 * SCALE, 16 * SCALE, 112 * SCALE, -16 * SCALE,\
        68.3784 * SCALE, -29.4324 * SCALE);
    arc (112 * SCALE, -16 * SCALE, 64 * SCALE, -48 * SCALE,\
        68.3784 * SCALE, -2.56757 * SCALE);
    arc (176 * SCALE, 0 * SCALE, 176 * SCALE, -96 * SCALE,\
        176 * SCALE, -48 * SCALE);
    line (240 * SCALE, -48 * SCALE, 256 * SCALE, -48 * SCALE);
    line (176 * SCALE, -96 * SCALE, 112 * SCALE, -96 * SCALE);
    line (176 * SCALE, 0 * SCALE, 112 * SCALE, 0 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -96 * SCALE);
    circle (232 * SCALE, -48 * SCALE, 8 * SCALE);
    pin ("O", 256 * SCALE, -48 * SCALE, RIGHT);
    pin ("B2", 0 * SCALE, -96 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -64 * SCALE, LEFT);
    pin ("A2", 0 * SCALE, -32 * SCALE, LEFT);
    pin ("A1", 0 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "OAI22S" */
  symbol ("OR2") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "OR2" */
  symbol ("OR2B1") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OR2B1" */
  symbol ("OR2B1P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OR2B1P" */
  symbol ("OR2B1S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OR2B1S" */
  symbol ("OR2B1T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OR2B1T" */
  symbol ("OR2P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "OR2P" */
  symbol ("OR2S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "OR2S" */
  symbol ("OR2T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "OR2T" */
  symbol ("OR3") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    line (36 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "OR3" */
  symbol ("OR3B1") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (36 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OR3B1" */
  symbol ("OR3B1P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (36 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OR3B1P" */
  symbol ("OR3B1S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (36 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OR3B1S" */
  symbol ("OR3B1T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (36 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -80 * SCALE, LEFT);
  } /* end of symbol "OR3B1T" */
  symbol ("OR3B2") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (28 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -48 * SCALE, 20 * SCALE, -48 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "OR3B2" */
  symbol ("OR3B2P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (28 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -48 * SCALE, 20 * SCALE, -48 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "OR3B2P" */
  symbol ("OR3B2S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (28 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -48 * SCALE, 20 * SCALE, -48 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "OR3B2S" */
  symbol ("OR3B2T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (28 * SCALE, -48 * SCALE, 8 * SCALE);
    line (0 * SCALE, -48 * SCALE, 20 * SCALE, -48 * SCALE);
    circle (20 * SCALE, -80 * SCALE, 8 * SCALE);
    line (0 * SCALE, -80 * SCALE, 12 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
    pin ("B2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("B1", 0 * SCALE, -48 * SCALE, LEFT);
  } /* end of symbol "OR3B2T" */
  symbol ("OR3P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    line (36 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "OR3P" */
  symbol ("OR3S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    line (36 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "OR3S" */
  symbol ("OR3T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    line (36 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 28 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 28 * SCALE, -16 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "OR3T" */
  symbol ("PDI") {
    set_minimum_boundary (-48 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (-32 * SCALE, -64 * SCALE, -48 * SCALE, -48 * SCALE);
    line (-16 * SCALE, -48 * SCALE, -32 * SCALE, -64 * SCALE);
    line (-48 * SCALE, -48 * SCALE, -16 * SCALE, -48 * SCALE);
    line (-32 * SCALE, -32 * SCALE, -32 * SCALE, -48 * SCALE);
    line (-32 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (48 * SCALE, -68 * SCALE, 48 * SCALE, -80 * SCALE);
    circle (48 * SCALE, -60 * SCALE, 8 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("EB", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "PDI" */
  symbol ("PDIX") {
    set_minimum_boundary (-48 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (-32 * SCALE, -64 * SCALE, -48 * SCALE, -48 * SCALE);
    line (-16 * SCALE, -48 * SCALE, -32 * SCALE, -64 * SCALE);
    line (-48 * SCALE, -48 * SCALE, -16 * SCALE, -48 * SCALE);
    line (-32 * SCALE, -32 * SCALE, -32 * SCALE, -48 * SCALE);
    line (-32 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (48 * SCALE, -68 * SCALE, 48 * SCALE, -80 * SCALE);
    circle (48 * SCALE, -60 * SCALE, 8 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("EB", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "PDIX" */
  symbol ("PUI") {
    set_minimum_boundary (-64 * SCALE, -80 * SCALE, 112 * SCALE, 0 * SCALE);
    line (-48 * SCALE, -16 * SCALE, -16 * SCALE, -16 * SCALE);
    line (-32 * SCALE, -32 * SCALE, -32 * SCALE, -16 * SCALE);
    line (48 * SCALE, -80 * SCALE, 48 * SCALE, -48 * SCALE);
    line (88 * SCALE, -32 * SCALE, 112 * SCALE, -32 * SCALE);
    line (-32 * SCALE, -32 * SCALE, 0 * SCALE, -32 * SCALE);
    line (88 * SCALE, -32 * SCALE, 16 * SCALE, 0 * SCALE);
    line (16 * SCALE, 0 * SCALE, 16 * SCALE, -64 * SCALE);
    line (0 * SCALE, -32 * SCALE, 16 * SCALE, -32 * SCALE);
    line (16 * SCALE, -64 * SCALE, 88 * SCALE, -32 * SCALE);
    pin ("O", 112 * SCALE, -32 * SCALE, RIGHT);
    pin ("E", 48 * SCALE, -80 * SCALE, DOWN);
  } /* end of symbol "PUI" */
  symbol ("QDBHN") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (0 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CKB", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDBHN" */
  symbol ("QDBHS") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (24 * SCALE, -144 * SCALE, 8 * SCALE);
    line (0 * SCALE, -144 * SCALE, 16 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CKB", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDBHS" */
  symbol ("QDFFN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFFN" */
  symbol ("QDFFP") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFFP" */
  symbol ("QDFFRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFFRBN" */
  symbol ("QDFFRBP") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFFRBP" */
  symbol ("QDFFRBS") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFFRBS" */
  symbol ("QDFFRBT") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFFRBT" */
  symbol ("QDFFRSBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -24 * SCALE, 8 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("SB", 112 * SCALE, 0 * SCALE, UP);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFFRSBN" */
  symbol ("QDFFS") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, -32 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFFS" */
  symbol ("QDFZN") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -128 * SCALE, 48 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDFZN" */
  symbol ("QDFZP") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -128 * SCALE, 48 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDFZP" */
  symbol ("QDFZRBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFZRBN" */
  symbol ("QDFZRBP") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFZRBP" */
  symbol ("QDFZRBS") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFZRBS" */
  symbol ("QDFZRBT") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, -32 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFZRBT" */
  symbol ("QDFZRSBN") {
    set_minimum_boundary (0 * SCALE, -256 * SCALE, 224 * SCALE, 0 * SCALE);
    line (192 * SCALE, -80 * SCALE, 224 * SCALE, -80 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (112 * SCALE, -256 * SCALE, 112 * SCALE, -240 * SCALE);
    circle (112 * SCALE, -232 * SCALE, 8 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    circle (112 * SCALE, -24 * SCALE, 8 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (48 * SCALE, -176 * SCALE, 32 * SCALE, -192 * SCALE);
    line (32 * SCALE, -160 * SCALE, 48 * SCALE, -176 * SCALE);
    line (0 * SCALE, -176 * SCALE, 32 * SCALE, -176 * SCALE);
    line (32 * SCALE, -224 * SCALE, 32 * SCALE, -32 * SCALE);
    line (32 * SCALE, -32 * SCALE, 192 * SCALE, -32 * SCALE);
    line (192 * SCALE, -32 * SCALE, 192 * SCALE, -224 * SCALE);
    line (192 * SCALE, -224 * SCALE, 32 * SCALE, -224 * SCALE);
    pin ("Q", 224 * SCALE, -80 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("SB", 112 * SCALE, 0 * SCALE, UP);
    pin ("RB", 112 * SCALE, -256 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -176 * SCALE, LEFT);
  } /* end of symbol "QDFZRSBN" */
  symbol ("QDFZS") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (0 * SCALE, -112 * SCALE, 32 * SCALE, -112 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (0 * SCALE, -48 * SCALE, 32 * SCALE, -48 * SCALE);
    line (48 * SCALE, -144 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -128 * SCALE, 48 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (0 * SCALE, -144 * SCALE, 32 * SCALE, -144 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("TD", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("SEL", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDFZS" */
  symbol ("QDLHN") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDLHN" */
  symbol ("QDLHP") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDLHP" */
  symbol ("QDLHRBN") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDLHRBN" */
  symbol ("QDLHRBP") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDLHRBP" */
  symbol ("QDLHRBS") {
    set_minimum_boundary (0 * SCALE, -224 * SCALE, 224 * SCALE, 0 * SCALE);
    circle (112 * SCALE, -200 * SCALE, 8 * SCALE);
    line (112 * SCALE, -224 * SCALE, 112 * SCALE, -208 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("RB", 112 * SCALE, -224 * SCALE, DOWN);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDLHRBS" */
  symbol ("QDLHS") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 0 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDLHS" */
  symbol ("QDLHSN") {
    set_minimum_boundary (0 * SCALE, -192 * SCALE, 224 * SCALE, 32 * SCALE);
    line (112 * SCALE, 0 * SCALE, 112 * SCALE, 32 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (192 * SCALE, -48 * SCALE, 224 * SCALE, -48 * SCALE);
    line (32 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (32 * SCALE, -192 * SCALE, 32 * SCALE, 0 * SCALE);
    line (32 * SCALE, 0 * SCALE, 192 * SCALE, 0 * SCALE);
    line (192 * SCALE, 0 * SCALE, 192 * SCALE, -192 * SCALE);
    line (192 * SCALE, -192 * SCALE, 32 * SCALE, -192 * SCALE);
    pin ("Q", 224 * SCALE, -48 * SCALE, RIGHT);
    pin ("S", 112 * SCALE, 32 * SCALE, UP);
    pin ("D", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("CK", 0 * SCALE, -144 * SCALE, LEFT);
  } /* end of symbol "QDLHSN" */
  symbol ("RAM2") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (224 * SCALE, -112 * SCALE, 256 * SCALE, -112 * SCALE);
    line (224 * SCALE, -16 * SCALE, 256 * SCALE, -16 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 224 * SCALE, 32 * SCALE);
    line (224 * SCALE, 32 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    pin ("QBZ", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("QB", 256 * SCALE, -16 * SCALE, RIGHT);
    pin ("W", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("RD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "RAM2" */
  symbol ("RAM2S") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (224 * SCALE, -112 * SCALE, 256 * SCALE, -112 * SCALE);
    line (224 * SCALE, -16 * SCALE, 256 * SCALE, -16 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 224 * SCALE, 32 * SCALE);
    line (224 * SCALE, 32 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    pin ("QBZ", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("QB", 256 * SCALE, -16 * SCALE, RIGHT);
    pin ("W", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("RD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "RAM2S" */
  symbol ("RAM3") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (224 * SCALE, -112 * SCALE, 256 * SCALE, -112 * SCALE);
    line (224 * SCALE, -16 * SCALE, 256 * SCALE, -16 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 224 * SCALE, 32 * SCALE);
    line (224 * SCALE, 32 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    pin ("QZ", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("Q", 256 * SCALE, -16 * SCALE, RIGHT);
    pin ("W", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("RD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "RAM3" */
  symbol ("RAM3S") {
    set_minimum_boundary (0 * SCALE, -160 * SCALE, 256 * SCALE, 32 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (224 * SCALE, -112 * SCALE, 256 * SCALE, -112 * SCALE);
    line (224 * SCALE, -16 * SCALE, 256 * SCALE, -16 * SCALE);
    line (32 * SCALE, -160 * SCALE, 32 * SCALE, 32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 224 * SCALE, 32 * SCALE);
    line (224 * SCALE, 32 * SCALE, 224 * SCALE, -160 * SCALE);
    line (224 * SCALE, -160 * SCALE, 32 * SCALE, -160 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (0 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    pin ("QZ", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("Q", 256 * SCALE, -16 * SCALE, RIGHT);
    pin ("W", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("RD", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "RAM3S" */
  symbol ("RAM5") {
    set_minimum_boundary (0 * SCALE, -208 * SCALE, 256 * SCALE, 32 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -208 * SCALE, 32 * SCALE, 32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 224 * SCALE, 32 * SCALE);
    line (224 * SCALE, 32 * SCALE, 224 * SCALE, -208 * SCALE);
    line (224 * SCALE, -208 * SCALE, 32 * SCALE, -208 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (224 * SCALE, -112 * SCALE, 256 * SCALE, -112 * SCALE);
    line (224 * SCALE, -16 * SCALE, 256 * SCALE, -16 * SCALE);
    line (0 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    pin ("QZ1", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("QZ0", 256 * SCALE, -16 * SCALE, RIGHT);
    pin ("W", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("RD1", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RD0", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "RAM5" */
  symbol ("RAM5S") {
    set_minimum_boundary (0 * SCALE, -208 * SCALE, 256 * SCALE, 32 * SCALE);
    line (32 * SCALE, -144 * SCALE, 0 * SCALE, -144 * SCALE);
    line (32 * SCALE, -112 * SCALE, 0 * SCALE, -112 * SCALE);
    line (32 * SCALE, -208 * SCALE, 32 * SCALE, 32 * SCALE);
    line (32 * SCALE, 32 * SCALE, 224 * SCALE, 32 * SCALE);
    line (224 * SCALE, 32 * SCALE, 224 * SCALE, -208 * SCALE);
    line (224 * SCALE, -208 * SCALE, 32 * SCALE, -208 * SCALE);
    line (0 * SCALE, -80 * SCALE, 32 * SCALE, -80 * SCALE);
    line (224 * SCALE, -112 * SCALE, 256 * SCALE, -112 * SCALE);
    line (224 * SCALE, -16 * SCALE, 256 * SCALE, -16 * SCALE);
    line (0 * SCALE, -16 * SCALE, 32 * SCALE, -16 * SCALE);
    pin ("QZ1", 256 * SCALE, -112 * SCALE, RIGHT);
    pin ("QZ0", 256 * SCALE, -16 * SCALE, RIGHT);
    pin ("W", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("RD1", 0 * SCALE, -144 * SCALE, LEFT);
    pin ("RD0", 0 * SCALE, -112 * SCALE, LEFT);
    pin ("D", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "RAM5S" */
  symbol ("TIE0") {
    set_minimum_boundary (-128 * SCALE, -128 * SCALE, -32 * SCALE, -32 * SCALE);
    line (-80 * SCALE, -40 * SCALE, -80 * SCALE, -32 * SCALE);
    line (-68 * SCALE, -48 * SCALE, -88 * SCALE, -60 * SCALE);
    line (-68 * SCALE, -48 * SCALE, -80 * SCALE, -40 * SCALE);
    line (-76 * SCALE, -92 * SCALE, -76 * SCALE, -104 * SCALE);
    line (-92 * SCALE, -104 * SCALE, -60 * SCALE, -104 * SCALE);
    line (-88 * SCALE, -84 * SCALE, -76 * SCALE, -92 * SCALE);
    line (-88 * SCALE, -60 * SCALE, -68 * SCALE, -72 * SCALE);
    line (-68 * SCALE, -72 * SCALE, -88 * SCALE, -84 * SCALE);
    line (-76 * SCALE, -120 * SCALE, -60 * SCALE, -104 * SCALE);
    line (-92 * SCALE, -104 * SCALE, -76 * SCALE, -120 * SCALE);
    line (-100 * SCALE, -120 * SCALE, -92 * SCALE, -120 * SCALE);
    line (-100 * SCALE, -32 * SCALE, -92 * SCALE, -32 * SCALE);
    line (-116 * SCALE, -32 * SCALE, -108 * SCALE, -32 * SCALE);
    line (-76 * SCALE, -32 * SCALE, -84 * SCALE, -32 * SCALE);
    line (-76 * SCALE, -120 * SCALE, -84 * SCALE, -120 * SCALE);
    line (-60 * SCALE, -120 * SCALE, -68 * SCALE, -120 * SCALE);
    line (-60 * SCALE, -32 * SCALE, -68 * SCALE, -32 * SCALE);
    line (-44 * SCALE, -80 * SCALE, -44 * SCALE, -88 * SCALE);
    line (-44 * SCALE, -64 * SCALE, -44 * SCALE, -72 * SCALE);
    line (-44 * SCALE, -48 * SCALE, -44 * SCALE, -56 * SCALE);
    line (-44 * SCALE, -32 * SCALE, -52 * SCALE, -32 * SCALE);
    line (-44 * SCALE, -120 * SCALE, -52 * SCALE, -120 * SCALE);
    line (-44 * SCALE, -112 * SCALE, -44 * SCALE, -120 * SCALE);
    line (-44 * SCALE, -96 * SCALE, -44 * SCALE, -104 * SCALE);
    line (-44 * SCALE, -32 * SCALE, -44 * SCALE, -40 * SCALE);
    line (-116 * SCALE, -80 * SCALE, -116 * SCALE, -88 * SCALE);
    line (-116 * SCALE, -64 * SCALE, -116 * SCALE, -72 * SCALE);
    line (-116 * SCALE, -48 * SCALE, -116 * SCALE, -56 * SCALE);
    line (-116 * SCALE, -120 * SCALE, -108 * SCALE, -120 * SCALE);
    line (-116 * SCALE, -112 * SCALE, -116 * SCALE, -120 * SCALE);
    line (-116 * SCALE, -96 * SCALE, -116 * SCALE, -104 * SCALE);
    line (-116 * SCALE, -32 * SCALE, -116 * SCALE, -40 * SCALE);
    pin ("O", -80 * SCALE, -32 * SCALE, UP);
  } /* end of symbol "TIE0" */
  symbol ("TIE1") {
    set_minimum_boundary (-32 * SCALE, -32 * SCALE, 64 * SCALE, 64 * SCALE);
    line (16 * SCALE, 32 * SCALE, 16 * SCALE, 48 * SCALE);
    line (0 * SCALE, 48 * SCALE, 32 * SCALE, 48 * SCALE);
    line (16 * SCALE, -16 * SCALE, 16 * SCALE, -32 * SCALE);
    line (8 * SCALE, 4 * SCALE, 28 * SCALE, -8 * SCALE);
    line (8 * SCALE, 28 * SCALE, 28 * SCALE, 16 * SCALE);
    line (28 * SCALE, 16 * SCALE, 8 * SCALE, 4 * SCALE);
    line (28 * SCALE, -8 * SCALE, 16 * SCALE, -16 * SCALE);
    line (8 * SCALE, 28 * SCALE, 16 * SCALE, 32 * SCALE);
    line (20 * SCALE, -32 * SCALE, 12 * SCALE, -32 * SCALE);
    line (36 * SCALE, -32 * SCALE, 28 * SCALE, -32 * SCALE);
    line (-4 * SCALE, -32 * SCALE, 4 * SCALE, -32 * SCALE);
    line (20 * SCALE, 56 * SCALE, 12 * SCALE, 56 * SCALE);
    line (36 * SCALE, 56 * SCALE, 28 * SCALE, 56 * SCALE);
    line (-4 * SCALE, 56 * SCALE, 4 * SCALE, 56 * SCALE);
    line (52 * SCALE, 8 * SCALE, 52 * SCALE, 0 * SCALE);
    line (52 * SCALE, 24 * SCALE, 52 * SCALE, 16 * SCALE);
    line (52 * SCALE, 40 * SCALE, 52 * SCALE, 32 * SCALE);
    line (52 * SCALE, 56 * SCALE, 44 * SCALE, 56 * SCALE);
    line (52 * SCALE, -32 * SCALE, 44 * SCALE, -32 * SCALE);
    line (52 * SCALE, -24 * SCALE, 52 * SCALE, -32 * SCALE);
    line (52 * SCALE, -8 * SCALE, 52 * SCALE, -16 * SCALE);
    line (52 * SCALE, 56 * SCALE, 52 * SCALE, 48 * SCALE);
    line (-20 * SCALE, 8 * SCALE, -20 * SCALE, 0 * SCALE);
    line (-20 * SCALE, 24 * SCALE, -20 * SCALE, 16 * SCALE);
    line (-20 * SCALE, 40 * SCALE, -20 * SCALE, 32 * SCALE);
    line (-20 * SCALE, 56 * SCALE, -12 * SCALE, 56 * SCALE);
    line (-20 * SCALE, -32 * SCALE, -12 * SCALE, -32 * SCALE);
    line (-20 * SCALE, -24 * SCALE, -20 * SCALE, -32 * SCALE);
    line (-20 * SCALE, -8 * SCALE, -20 * SCALE, -16 * SCALE);
    line (-20 * SCALE, 56 * SCALE, -20 * SCALE, 48 * SCALE);
    pin ("O", 16 * SCALE, -32 * SCALE, DOWN);
  } /* end of symbol "TIE1" */
  symbol ("XNR2H") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XNR2H" */
  symbol ("XNR2HP") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XNR2HP" */
  symbol ("XNR2HS") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XNR2HS" */
  symbol ("XNR2HT") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XNR2HT" */
  symbol ("XNR3") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (28 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (88 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XNR3" */
  symbol ("XNR3P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (28 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (88 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XNR3P" */
  symbol ("XNR3S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (28 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (88 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XNR3S" */
  symbol ("XNR3T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    line (88 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (28 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (16 * SCALE, -80 * SCALE, 0 * SCALE, -80 * SCALE);
    line (16 * SCALE, -16 * SCALE, 0 * SCALE, -16 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XNR3T" */
  symbol ("XNR4") {
    set_minimum_boundary (-16 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (16 * SCALE, -96 * SCALE, -12 * SCALE, -96 * SCALE);
    line (32 * SCALE, -64 * SCALE, -12 * SCALE, -64 * SCALE);
    line (32 * SCALE, -32 * SCALE, -12 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -12 * SCALE, 0 * SCALE);
    line (88 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", -16 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", -16 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", -16 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", -16 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "XNR4" */
  symbol ("XNR4P") {
    set_minimum_boundary (-16 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (16 * SCALE, -96 * SCALE, -12 * SCALE, -96 * SCALE);
    line (32 * SCALE, -64 * SCALE, -12 * SCALE, -64 * SCALE);
    line (32 * SCALE, -32 * SCALE, -12 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -12 * SCALE, 0 * SCALE);
    line (88 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", -16 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", -16 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", -16 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", -16 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "XNR4P" */
  symbol ("XNR4S") {
    set_minimum_boundary (-16 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (16 * SCALE, -96 * SCALE, -12 * SCALE, -96 * SCALE);
    line (32 * SCALE, -64 * SCALE, -12 * SCALE, -64 * SCALE);
    line (32 * SCALE, -32 * SCALE, -12 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -12 * SCALE, 0 * SCALE);
    line (88 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", -16 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", -16 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", -16 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", -16 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "XNR4S" */
  symbol ("XNR4T") {
    set_minimum_boundary (-16 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (16 * SCALE, -96 * SCALE, -12 * SCALE, -96 * SCALE);
    line (32 * SCALE, -64 * SCALE, -12 * SCALE, -64 * SCALE);
    line (32 * SCALE, -32 * SCALE, -12 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -12 * SCALE, 0 * SCALE);
    line (88 * SCALE, -96 * SCALE, 16 * SCALE, -96 * SCALE);
    line (80 * SCALE, 0 * SCALE, 16 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    circle (152 * SCALE, -48 * SCALE, 8 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", -16 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", -16 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", -16 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", -16 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "XNR4T" */
  symbol ("XOR2H") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XOR2H" */
  symbol ("XOR2HP") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XOR2HP" */
  symbol ("XOR2HS") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XOR2HS" */
  symbol ("XOR2HT") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I2", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XOR2HT" */
  symbol ("XOR3") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (28 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XOR3" */
  symbol ("XOR3P") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (28 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XOR3P" */
  symbol ("XOR3S") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (28 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XOR3S" */
  symbol ("XOR3T") {
    set_minimum_boundary (0 * SCALE, -96 * SCALE, 160 * SCALE, 0 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    line (28 * SCALE, -48 * SCALE, 0 * SCALE, -48 * SCALE);
    line (0 * SCALE, -80 * SCALE, 16 * SCALE, -80 * SCALE);
    line (0 * SCALE, -16 * SCALE, 16 * SCALE, -16 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I3", 0 * SCALE, -80 * SCALE, LEFT);
    pin ("I2", 0 * SCALE, -48 * SCALE, LEFT);
    pin ("I1", 0 * SCALE, -16 * SCALE, LEFT);
  } /* end of symbol "XOR3T" */
  symbol ("XOR4") {
    set_minimum_boundary (-16 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (12 * SCALE, -96 * SCALE, -12 * SCALE, -96 * SCALE);
    line (28 * SCALE, -64 * SCALE, -12 * SCALE, -64 * SCALE);
    line (28 * SCALE, -32 * SCALE, -12 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -12 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", -16 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", -16 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", -16 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", -16 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "XOR4" */
  symbol ("XOR4P") {
    set_minimum_boundary (-16 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (12 * SCALE, -96 * SCALE, -12 * SCALE, -96 * SCALE);
    line (28 * SCALE, -64 * SCALE, -12 * SCALE, -64 * SCALE);
    line (28 * SCALE, -32 * SCALE, -12 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -12 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", -16 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", -16 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", -16 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", -16 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "XOR4P" */
  symbol ("XOR4S") {
    set_minimum_boundary (-16 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (12 * SCALE, -96 * SCALE, -12 * SCALE, -96 * SCALE);
    line (28 * SCALE, -64 * SCALE, -12 * SCALE, -64 * SCALE);
    line (28 * SCALE, -32 * SCALE, -12 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -12 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", -16 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", -16 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", -16 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", -16 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "XOR4S" */
  symbol ("XOR4T") {
    set_minimum_boundary (-16 * SCALE, -96 * SCALE, 160 * SCALE, 16 * SCALE);
    line (12 * SCALE, -96 * SCALE, -12 * SCALE, -96 * SCALE);
    line (28 * SCALE, -64 * SCALE, -12 * SCALE, -64 * SCALE);
    line (28 * SCALE, -32 * SCALE, -12 * SCALE, -32 * SCALE);
    line (12 * SCALE, 0 * SCALE, -12 * SCALE, 0 * SCALE);
    arc (140 * SCALE, -48 * SCALE, 88 * SCALE, -96 * SCALE,\
        88.752 * SCALE, -44.648 * SCALE);
    arc (8 * SCALE, 0 * SCALE, 8 * SCALE, -96 * SCALE,\
        -39.6 * SCALE, -48 * SCALE);
    line (160 * SCALE, -48 * SCALE, 144 * SCALE, -48 * SCALE);
    line (16 * SCALE, -96 * SCALE, 88 * SCALE, -96 * SCALE);
    line (16 * SCALE, 0 * SCALE, 80 * SCALE, 0 * SCALE);
    arc (80 * SCALE, 0 * SCALE, 144 * SCALE, -48 * SCALE,\
        85.0197 * SCALE, -59.9737 * SCALE);
    arc (16 * SCALE, 0 * SCALE, 16 * SCALE, -96 * SCALE,\
        -31.6 * SCALE, -48 * SCALE);
    pin ("O", 160 * SCALE, -48 * SCALE, RIGHT);
    pin ("I4", -16 * SCALE, -96 * SCALE, LEFT);
    pin ("I3", -16 * SCALE, -64 * SCALE, LEFT);
    pin ("I2", -16 * SCALE, -32 * SCALE, LEFT);
    pin ("I1", -16 * SCALE, 0 * SCALE, LEFT);
  } /* end of symbol "XOR4T" */
}
